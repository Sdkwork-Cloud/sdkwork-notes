# services/

This directory is reserved per the SDKWork root dictionary. Notes product/service crates live under [../crates/](../crates/) instead:

| Crate | Role |
| --- | --- |
| `sdkwork-notes-pages-service` | Notes domain service and Drive content port |

HTTP adapters are in [../crates/](../crates/). SQLx persistence is in [../crates/sdkwork-notes-pages-repository-sqlx](../crates/sdkwork-notes-pages-repository-sqlx/).
