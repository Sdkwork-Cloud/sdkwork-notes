# sdkwork-notes-pc-desktop

Tauri native host for SDKWork Notes PC desktop and tablet targets.

This README is the SDKWork module entrypoint for `sdkwork-notes-pc-desktop`. The machine-readable component contract is `specs/component.spec.json`; canonical standards are under `../../../../../sdkwork-specs/`.
