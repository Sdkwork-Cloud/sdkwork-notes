> Migrated from `docs/review/step-04-重命名文件夹写路径编排收敛-2026-04-07.md` on 2026-06-24.
> Owner: SDKWork maintainers

# Step 04-重命名文件夹写路径编排收敛

- 日期：`2026-04-07`
- 轮次：`Wave-B / 第十四轮推进`
- 当前 Step：`04-工作区边界收敛与数据访问抽象`
- 当前等级：`L3`
- 本轮结论：`真实增量已落地，但 Step 04 仍未达到 L4`

## 1. 本轮目标

本轮的目标不是继续扩散功能面，而是继续压缩 `useNotesWorkspaceStore.ts` 中残留的写路径编排职责，把 `renameFolder()` 的远程调用、结果归一化和状态计划拼装下沉到显式 coordinator。

目标判定标准：

1. Store 不再直接组合 `workspaceService.renameFolder()` 与 `planRenamedFolderState()`。
2. 写路径协作链具备独立的 Node contract，可先红灯证明缺口，再转绿证明实现。
3. 变更后不改变当前 Step 的真实分级判断，仍以事实为准维持 `L3`。

## 2. 实施结果

本轮已完成以下落地：

1. 在 `packages/sdkwork-notes-notes/src/services/noteWorkspaceWriteCoordinator.ts` 中新增 `NoteWorkspaceRenameFolderStateResult`，并为 coordinator 注入 `renameFolder` 依赖。
2. 新增 `renameFolderState()`，统一承担“远程重命名 -> 返回 id 归一化 -> `planRenamedFolderState()` 拼装 -> 错误结果归一化”的写路径编排职责。
3. 在 `packages/sdkwork-notes-notes/src/store/useNotesWorkspaceStore.ts` 中，将 `renameFolder()` 改为只消费 coordinator 返回结果；store 不再直接承接远程调用和 rename plan 组装。
4. 在 `sdkwork-notes-pc-react/scripts/workspace-write-path.contract.test.mjs` 中新增 `renameFolderState()` contract，并先验证红灯失败原因为 `coordinator.renameFolderState is not a function`，随后完成实现转绿。

## 3. 验证结果

本轮验证通过：

```powershell
node --test --experimental-test-isolation=none scripts/workspace-write-path.contract.test.mjs
pnpm.cmd --filter @sdkwork/notes-notes typecheck
pnpm.cmd typecheck
```

验证结论：

1. `workspace-write-path.contract.test.mjs` 已能够覆盖 `renameFolderState()` 的完整协作链。
2. 包级 TypeScript 编译通过，说明 coordinator/store 接口重构没有破坏类型边界。
3. 根级 `pnpm.cmd typecheck` 通过，说明工作区合同链与包图仍然成立。

补充说明：

- `pnpm.cmd --filter @sdkwork/notes-notes test -- src/store/useNotesWorkspaceStore.test.ts` 在当前环境仍受 `spawn EPERM` 限制，属于已知环境问题，不作为本轮回归门禁依据。

## 4. 评估标准与结论

### 4.1 边界收敛评估

- 评估标准：store 是否只做状态应用，而不再直接承担远程 mutation 编排。
- 本轮结果：达到本轮目标，`renameFolder()` 已切换为消费 coordinator。

### 4.2 正确性评估

- 评估标准：重命名后是否能同步维护 folder id、子级 parentId、note parentId、trash parentId、activeNote、selectedFolderId、expandedFolderIds。
- 本轮结果：已通过 contract 明确锁定上述行为。

### 4.3 工程门禁评估

- 评估标准：是否具备可重复执行的红绿验证证据，且变更可纳入现有合同链。
- 本轮结果：满足，Node contract 与 typecheck 均已通过。

### 4.4 Step 完成度评估

- 评估标准：是否已经完成 Step 04 的剩余写路径收口，并具备 L4 退出条件。
- 本轮结果：未达到。虽然 `renameFolder` 已收口，但 `moveNote` 仍是当前最明确的残留写路径。

## 5. 审计结论

本轮之后可以确认：

1. write coordinator 已从 create 系列扩展到 rename 系列，边界工程成熟度继续提升。
2. Store 的写路径职责进一步缩小，Step 04 的“页面状态容器过载”问题继续被实质性削弱。
3. 当前最准确状态仍然是 `Step 04 / L3`，不能因为又完成一条增量就提前宣布完成。

## 6. 剩余缺口

当前仍需继续收口的核心问题：

1. `moveNote` 写路径仍保留在 store 中，尚未完成与 `renameFolder` 同等级的编排下沉。
2. repository 仅完成能力显式化，尚未形成 `read-through / replica / queued-sync` 的策略接缝。
3. 页面容器层仍保留少量 icon / layout / status 胶水，Step 04 还未彻底退化为纯装配层。

## 7. 下一轮建议

建议下一轮继续留在 Step 04，并按以下顺序推进：

1. 优先收口 `moveNote` 写路径。
2. 其次开始为 repository 的未来同步策略预埋可扩展接口。
3. 最后继续压缩页面容器残留胶水，确保 Step 04 的退出标准能够被明确验证。

