> Migrated from `docs/step/06-启动恢复smoke补齐-2026-04-13.md` on 2026-06-24.
> Owner: SDKWork maintainers

# Step 06 启动恢复 Smoke 补齐

- 日期: `2026-04-13`
- 轮次: `Wave-B / 第三十四轮推进`
- 关联能力: `CP06-4 / 启动恢复 smoke test`

## 目标

为已经存在的本地草稿启动恢复链路补齐最后一块回归证据，确认以下流程可稳定工作：

1. 启动读取远端 workspace
2. 读取本地 snapshot
3. 将本地 draft 映射到 live note
4. 为活动 note 解析 `activeRecoveredDraft`
5. 在异常 payload 或读取失败时安全降级

## 本次改动

### 合同测试

- `sdkwork-notes-pc-react/scripts/workspace-startup-recovery-smoke.contract.test.mjs`

### 门禁接入

- `sdkwork-notes-pc-react/package.json`
- `sdkwork-notes-pc-react/scripts/package-scripts-contract.test.mjs`

## 场景矩阵

1. 当前 envelope + live note draft
2. legacy raw snapshot + live note draft
3. unknown version payload
4. corrupted payload
5. `loadWorkspace()` rejection
6. trashed / missing draft filtering

## 结果

### 能力状态

1. `CP06-1 = L4`
2. `CP06-2 = L4`
3. `CP06-3 = L4`
4. `CP06-4 = L4`
5. `Step 06 = L4`

### 判断依据

1. 启动恢复 smoke matrix 已纳入根级 contract 回归。
2. 现有生产代码无需再为 `CP06-4` 扩展新逻辑，说明前序设计已成立。
3. 远端 live note、trash note、本地 snapshot 兼容、异常 payload 降级均已有可执行证据。

## 验证命令

```powershell
node --test --experimental-test-isolation=none scripts/workspace-startup-recovery-smoke.contract.test.mjs
node --test --experimental-test-isolation=none scripts/workspace-local-schema.contract.test.mjs
node --test --experimental-test-isolation=none scripts/workspace-local-recovery.contract.test.mjs
node --test --experimental-test-isolation=none scripts/package-scripts-contract.test.mjs
pnpm.cmd typecheck
```

