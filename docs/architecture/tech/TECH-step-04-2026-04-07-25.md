> Migrated from `docs/review/step-04-页面展示模型收敛-2026-04-07.md` on 2026-06-24.
> Owner: SDKWork maintainers

# Step 04 页面展示模型收敛 Review

## 1. 本轮目标

本轮继续执行 `Step 04-工作区边界收敛与数据访问抽象`，但不再重复处理已经落地的读取策略边界，而是转向 `NotesWorkspacePage.tsx` 中仍然残留的展示层胶水。目标是把快捷键提示、统计卡片、焦点卡片的展示拼装，从页面组件内联计算下沉到可测试的纯模型服务。

## 2. 实际落地

### 新增

- `sdkwork-notes-pc-react/packages/sdkwork-notes-notes/src/services/noteWorkspacePagePresentationModel.ts`
  - 新增 `buildNotesWorkspacePagePresentationModel()`
  - 新增 `resolveNotesWorkspaceModifierKey()`
  - 新增 `resolveNotesWorkspacePublishStatusChipClass()`
- `sdkwork-notes-pc-react/scripts/workspace-page-presentation-model.contract.test.mjs`
  - 新增页面展示模型的 Node contract
- `sdkwork-notes-pc-react/package.json`
  - 将 `workspace-page-presentation-model.contract.test.mjs` 纳入 `test:workspace:contracts`
- `sdkwork-notes-pc-react/scripts/package-scripts-contract.test.mjs`
  - 同步冻结新的脚本聚合链

### 变更

- `sdkwork-notes-pc-react/packages/sdkwork-notes-notes/src/pages/NotesWorkspacePage.tsx`
  - 页面改为消费 `pagePresentation` 结构化模型
  - 快捷键提示不再内联拼接 `Cmd/Ctrl`
  - 统计卡片不再在页面层手工装配 label/value/icon 对
  - 焦点卡片的标题、描述、badge、detail 不再由页面内联散落计算
- `sdkwork-notes-pc-react/packages/sdkwork-notes-notes/src/services/index.ts`
  - 导出页面展示模型服务

## 3. TDD 证据

### RED

先新增 `workspace-page-presentation-model.contract.test.mjs`，随后执行：

```powershell
node --test --experimental-test-isolation=none scripts/workspace-page-presentation-model.contract.test.mjs
```

首轮失败原因为：

- `ENOENT: ... noteWorkspacePagePresentationModel.ts`

这说明测试确实锁定了本轮待交付的目标模块，而不是误测已有能力。

### GREEN

完成 `noteWorkspacePagePresentationModel.ts` 与页面接线后，同一命令转绿。

## 4. 验证结果

本轮已通过：

```powershell
node --test --experimental-test-isolation=none scripts/workspace-page-presentation-model.contract.test.mjs
node --test --experimental-test-isolation=none scripts/package-scripts-contract.test.mjs
pnpm.cmd --filter @sdkwork/notes-notes typecheck
pnpm.cmd typecheck
```

补充说明：

- `pnpm.cmd typecheck` 已完整经过 `test:workspace:contracts` 全链。
- 包内 Vitest 在当前环境仍受 `spawn EPERM` 约束，不作为本轮结论依据。

## 5. 审计结论

### 本轮收益

1. 页面顶部快捷键提示从 UI 层字符串拼装，收敛为纯展示模型输出。
2. 焦点卡片的 badge/status/detail 已具备独立 contract，可在不加载 React 运行时的情况下回归验证。
3. `NotesWorkspacePage.tsx` 的职责进一步压缩为“装配 store/runtime + 渲染模型”。

### 当前准确状态

- Step 04 仍然是 `L3`
- 当前总评维持 `98 / 100`

### 仍未关闭的残留

1. 页面容器仍保留命令面板 icon 映射与部分 header action/layout 装配胶水。
2. repository 虽已具备读取策略边界，但尚未建立面向未来 read-through / replica / queued-sync 的策略接缝。
3. 因此本轮是“页面展示层再收一层”，不是 Step 04 完结信号。

