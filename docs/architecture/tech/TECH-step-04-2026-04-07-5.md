> Migrated from `docs/review/step-04-创建笔记写路径编排收敛-2026-04-07.md` on 2026-06-24.
> Owner: SDKWork maintainers

# Step 04 创建笔记写路径编排收敛

- 日期：`2026-04-07`
- Step：`04-工作区边界收敛与数据访问抽象`
- 波次：`Wave-B / 第十二轮推进`
- 当前等级：`L3`

## 1. 本轮目标

本轮不扩张能力面，只继续压缩 `useNotesWorkspaceStore.ts` 中仍然残留的写路径编排逻辑。优先选择 `createNote()`，因为它同时承担了以下四类职责：

1. 调用 `workspaceService.save()` 发起创建。
2. 调用 `workspaceService.findById()` 回填详情。
3. 调用 `planCreatedNoteState()` 回放状态变更。
4. 在 store 内部拼装错误分支和返回值。

这段逻辑仍然属于 Step 04 需要继续下沉的“写路径编排残留”，但粒度足够小，适合作为本轮最小真实增量。

## 2. TDD 执行记录

### 2.1 先写失败契约

先扩展 `sdkwork-notes-pc-react/scripts/workspace-write-path.contract.test.mjs`，增加以下契约：

- `createNotesWorkspaceWriteCoordinator()` 必须对外暴露 `createNoteState()`。
- `createNoteState()` 必须先持久化、再回填详情、再返回创建后的状态计划。
- 返回结果必须包含 `createdNoteId / activeNoteId / activeNote / selectedFolderId / expandedFolderIds / notes / trashedNotes`。

红灯验证命令：

```powershell
node --test --experimental-test-isolation=none scripts/workspace-write-path.contract.test.mjs
```

首次失败原因：

- `workspaceWritePathModule.createNotesWorkspaceWriteCoordinator is not a function`

失败原因与预期一致，说明测试锁定的是“缺少写路径编排边界实现”，而不是测试自身错误。

### 2.2 绿色实现

随后在 `packages/sdkwork-notes-notes/src/services/noteWorkspaceWriteCoordinator.ts` 中实现：

- `createNotesWorkspaceWriteCoordinator()`
- `createNoteState()`

实现职责边界如下：

1. 接收 `saveNote / findNoteDetail` 依赖。
2. 先执行创建持久化。
3. 失败时返回统一错误结果。
4. 成功时回填详情；如果详情缺失，则回落到创建摘要 + 输入内容。
5. 最后统一通过 `planCreatedNoteState()` 生成状态计划。

### 2.3 Store 接线

在 `packages/sdkwork-notes-notes/src/store/useNotesWorkspaceStore.ts` 中：

- 移除 `createNote()` 内部对 `workspaceService.save()` 与 `workspaceService.findById()` 的直接编排。
- 改为消费 `createNotesWorkspaceWriteCoordinator().createNoteState()`。
- store 仅保留：
  - 脏草稿前置持久化守卫
  - 结果写回
  - `saveState / errorMessage` 这类状态层职责

## 3. 修改清单

### 代码

- `sdkwork-notes-pc-react/packages/sdkwork-notes-notes/src/services/noteWorkspaceWriteCoordinator.ts`
- `sdkwork-notes-pc-react/packages/sdkwork-notes-notes/src/store/useNotesWorkspaceStore.ts`
- `sdkwork-notes-pc-react/scripts/workspace-write-path.contract.test.mjs`

### 文档

- `docs/review/step-04-创建笔记写路径编排收敛-2026-04-07.md`
- `docs/架构/06-业务流程-应用接口与集成设计-创建笔记写路径补充-2026-04-07.md`
- `docs/架构/10-实施进度-创建笔记写路径增量-2026-04-07.md`
- `docs/release/Step04-创建笔记写路径编排收敛-2026-04-07.md`

## 4. 验证结果

### 通过

```powershell
node --test --experimental-test-isolation=none scripts/workspace-write-path.contract.test.mjs
pnpm.cmd --filter @sdkwork/notes-notes typecheck
pnpm.cmd typecheck
```

### 受环境限制但非本轮回归

```powershell
pnpm.cmd --filter @sdkwork/notes-notes test -- src/store/useNotesWorkspaceStore.test.ts
```

当前环境中的 Vitest 仍受 `spawn EPERM` 限制，失败模式与前几轮一致，不构成本轮回归证据。

## 5. 评估结论

### 本轮收益

1. `createNote` 写路径第一次从“store 内嵌异步编排”下沉为显式 service/coordinator 边界。
2. 写路径 contract 不再只覆盖纯状态规划，也开始覆盖“持久化 + 回填 + 状态计划”的真实协作链。
3. Step 04 的收敛方向更清晰：接下来可按同一模式继续处理 `createFolder / renameFolder / moveNote`。

### 当前真实状态

- `Step 04 = 进行中`
- `当前等级 = L3`

### 仍未关闭的阻塞

1. `createFolder / renameFolder / moveNote` 仍有相似的 store 写路径编排残留。
2. repository 策略层尚未进入 read-through / replica / queued-sync 等更高阶抽象。
3. `NotesWorkspacePage.tsx` 仍有少量 icon、状态样式、容器装配胶水未完全收口。

因此，本轮属于真实增量，但还不足以宣布 Step 04 收口到 `L4`。

