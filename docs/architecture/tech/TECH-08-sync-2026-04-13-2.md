> Migrated from `docs/step/08-待同步队列与重试机制落地-2026-04-13.md` on 2026-06-24.
> Owner: SDKWork maintainers

# Step 08 - 待同步队列与重试机制落地

- 日期：`2026-04-13`
- 波次：`Wave-C / 第四十轮推进`
- 检查点：`CP08-2 / 待同步队列与重试机制落地`

## 交付范围

- 在 `@sdkwork/notes-sync` 内冻结版本化待同步队列快照协议，明确 `storage key / schema version / queue snapshot / envelope`。
- 提供浏览器侧 queue store，保证待同步任务可以稳定持久化，并在异常 payload、未知 schema 版本下安全降级为空队列。
- 提供重试策略与回放辅助接口，把“自动重试 -> 到点回放 -> 超限失败”从调用侧零散逻辑收束为包内显式 API。
- 保持范围仅在同步包，不提前接入 `notes-notes` 主写入路径或同步状态 UI。

## 变更摘要

### 新增

- `sdkwork-notes-pc-react/scripts/workspace-sync-queue.contract.test.mjs`
  - 冻结版本化队列快照持久化行为
  - 冻结异常 payload / 未知 schema 版本的降级语义
  - 冻结自动重试、超限失败与回放回队列语义

### 修改

- `sdkwork-notes-pc-react/packages/sdkwork-notes-sync/src/index.ts`
  - 新增 `NOTES_SYNC_QUEUE_STORAGE_KEY / NOTES_SYNC_QUEUE_SCHEMA_VERSION`
  - 新增 `NotesSyncQueueSnapshot / NotesSyncQueueEnvelope / NotesSyncQueueStore`
  - 新增 `createBrowserNotesSyncQueueStore()`
  - 新增 `createNotesSyncRetryPolicy()`
  - 新增 `scheduleNotesSyncTaskRetry()`、`releaseNotesSyncTaskForReplay()`
- `sdkwork-notes-pc-react/package.json`
- `sdkwork-notes-pc-react/scripts/package-scripts-contract.test.mjs`

## 兑现结果

- `notes-sync` 不再只有状态机契约，还具备待同步队列持久化和自动重试的最小可落地边界。
- 队列可以以版本化快照形式落到 browser storage，并在损坏数据或未知版本下自动降级，避免把垃圾状态带入后续同步流程。
- 重试 delay ladder、自动回放和终态失败被明确收口，`CP08-3` 可以直接复用这些边界接入主写路径。

## 当前状态

- `CP08-1 / 同步任务模型与状态机冻结 = L4`
- `CP08-2 / 待同步队列与重试机制落地 = L4`
- `CP08-3 / 主写入路径接入 = 待开始`
- `CP08-4 / 冲突与失败恢复验证 = 待开始`
- `Step 08` 处于 `CP08-1 / CP08-2 已闭环、整体进行中`

## 验证

```powershell
node --test --experimental-test-isolation=none scripts/workspace-sync-state-machine.contract.test.mjs
node --test --experimental-test-isolation=none scripts/workspace-sync-queue.contract.test.mjs
node --test --experimental-test-isolation=none scripts/package-scripts-contract.test.mjs
pnpm.cmd --filter @sdkwork/notes-sync typecheck
pnpm.cmd typecheck
```

