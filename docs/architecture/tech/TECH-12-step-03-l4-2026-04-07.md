> Migrated from `docs/架构/12-Step-03-L4收口-2026-04-07.md` on 2026-06-24.
> Owner: SDKWork maintainers

# 12. Step 03 L4 收口记录 - 2026-04-07

## 12.1 目标

记录 Step 03 从“会话安全与配置治理待收口”推进到 `L4` 的最终事实，避免后续继续沿用“会话仍主要落在 localStorage”或“环境配置优先级未冻结”的过期结论。

## 12.2 收口原因

Step 03 的原始目标是解决三类基础风险：

- 会话令牌长期散落在低安全级浏览器持久层
- 运行时配置来源、覆盖顺序和 owner scope 缺乏权威定义
- Desktop 与 Web 在会话持久化和启动期水合路径上缺少统一边界

本轮已完成以下收口动作：

- `notes-core`
  - 新增统一会话存储适配接口 `configureAppSdkSessionStoreAdapter`
  - 新增运行时配置解析入口 `resolveAppSdkRuntimeContext`
  - Web 默认会话存储从 `localStorage` 切换为 `sessionStorage`
  - 保留旧 `localStorage` 会话键的一次性迁移能力，并在迁移后立即清理旧键
- `notes-desktop`
  - 新增 `sessionBridge.ts`，把桌面前端会话镜像与 Tauri 原生会话状态串行同步
  - Desktop 启动期在 `createDesktopApp()` 中安装原生会话桥
  - Tauri 新增 `read_session_state / write_session_state / clear_session_state` 命令
  - 原生权威副本冻结为 app data 目录中的 `desktop-session.json`
- 环境配置
  - `.env.example`
  - `.env.development`
  - `.env.test`
  - `.env.production`
  - 以上文件已显式声明 `VITE_APP_OWNER_MODE=tenant`

## 12.3 收口证据

- 命令证据：
  - `node --test --experimental-test-isolation=none scripts/env-config-contract.test.mjs`
  - `node --test --experimental-test-isolation=none scripts/session-store-behavior.test.mjs`
  - `node --test --experimental-test-isolation=none scripts/desktop-session-bridge.contract.test.mjs`
  - `pnpm.cmd test:workspace:contracts`
  - `pnpm.cmd test:desktop:contracts`
  - `pnpm.cmd test:desktop:rust`
  - `pnpm.cmd typecheck`
- 代码证据：
  - `sdkwork-notes-pc-react/packages/sdkwork-notes-core/src/sdk/useAppSdkClient.ts`
  - `sdkwork-notes-pc-react/packages/sdkwork-notes-desktop/src/desktop/sessionBridge.ts`
  - `sdkwork-notes-pc-react/packages/sdkwork-notes-desktop/src/desktop/bootstrap/createDesktopApp.tsx`
  - `sdkwork-notes-pc-react/packages/sdkwork-notes-desktop/src-tauri/src/commands/session_state.rs`
  - `sdkwork-notes-pc-react/packages/sdkwork-notes-desktop/src-tauri/src/state/mod.rs`
  - `sdkwork-notes-pc-react/package.json`

## 12.4 结论

- Step 03 的历史阻塞项已全部解除。
- “会话仍主要依赖 localStorage”不再是当前有效描述。
- “配置来源优先级与 ownerMode 未冻结”不再是当前有效描述。
- Step 03 当前应以 `L4` 作为唯一有效等级。
- Step 04 可作为下一执行入口，不再需要继续停留在 Step 03 做基础治理返工。

