> Migrated from `docs/release/最新版本说明-第五十二轮推进补记-2026-04-14.md` on 2026-06-24.
> Owner: SDKWork maintainers

# 最新版本说明 - 第五十二轮推进补记 - 2026-04-14

## 本轮补记

- 已把 `NotesSyncTask` 从“弱任务元数据”推进到“带显式 `mutation` 的同步任务合同”。
- `upsert / move / delete / restore / permanent-delete` 现在都具备可审计的最小执行意图。
- 同步队列 schema 已升级到 `2`，旧的 schema 1 envelope 会显式降级为空队列。
- 本轮仍然没有接入默认 replay handler，这是刻意保持风险受控，而不是遗漏实现。

## 已完成

1. `notes-sync` 新增 `mutation` 合同与 queue schema 2。
2. `notes-notes` 已在所有已接入 note 主写入路径中写入显式 payload。
3. `state-machine / queue / write-path / worker / worker-runtime / runtime-boundary` Node contract 全部通过。

## 仍待完成

1. replay-safe transport / idempotency 边界。
2. 真正的 remote ack apply / `remoteCursor` 合并。
3. 默认 runtime caller。
4. conflict/manual replay UI。

