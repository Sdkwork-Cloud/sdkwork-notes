> Migrated from `docs/架构/11-Step-02-L4收口-2026-04-07.md` on 2026-06-24.
> Owner: SDKWork maintainers

# 11. Step 02 L4 收口记录 - 2026-04-07

## 11.1 目标

记录 Step 02 从 `L3` 升级到 `L4` 的最终收口事实，避免后续执行继续沿用“验证未闭环”的过期结论。

## 11.2 收口原因

Step 02 原先停留在 `L3`，原因是以下两类问题尚未闭环：

- 根级 `node --test` 入口在当前 Windows / PowerShell 环境下直接执行时触发 `spawn EPERM`
- turbo dry-run 合同测试依赖 stdout 管道抓取 JSON，在当前环境下无法稳定返回结果

本轮已完成以下修复：

- `package.json`
  - `test:workspace:contracts`
  - `test:desktop:contracts`
  - 统一切换为 `node --test --experimental-test-isolation=none`
- `scripts/internal-packages-turbo-contract.test.mjs`
  - 由 stdout 管道抓取改为文件描述符落盘 JSON 后读取
- `pnpm-lock.yaml`
  - 通过 `pnpm.cmd install --lockfile-only --ignore-scripts` 收敛 importer

## 11.3 收口证据

- 命令证据：
  - `pnpm.cmd test:workspace:contracts`
  - `pnpm.cmd test:desktop:contracts`
  - `pnpm.cmd typecheck`
- 脚本证据：
  - `sdkwork-notes-pc-react/package.json`
  - `sdkwork-notes-pc-react/scripts/package-scripts-contract.test.mjs`
  - `sdkwork-notes-pc-react/scripts/internal-packages-turbo-contract.test.mjs`
- lockfile 证据：
  - `pnpm-lock.yaml` 已包含五个新包 importer

## 11.4 结论

- Step 02 的历史阻塞项已全部解除。
- Step 02 当前应以 `L4` 作为唯一有效等级。
- Step 03 可作为下一执行入口，不再需要继续停留在 Step 02 做门禁修复。

