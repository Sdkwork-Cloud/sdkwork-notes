> Migrated from `docs/release/Step05-保存重试退避与观测-2026-04-13.md` on 2026-06-24.
> Owner: SDKWork maintainers

# Step05-保存重试退避与观测

- 日期：`2026-04-13`
- 版本：`0.1.0 / Unreleased`
- 阶段：`Step 05 / Wave-B / 第二十九轮后续增量`
- 范围：`保存链可靠性升级`

## 1. 变更摘要

本轮在既有 `save queue` 串行保存编排基础上，补齐了保存失败后的自动退避、重试上限与最小观测接口，使保存链从“可以串行执行”继续提升到“可以稳定恢复并可被观测”。

## 2. 已交付内容

1. 新增 `noteWorkspaceSaveRetryPolicy.ts`，冻结默认退避策略 `500ms -> 1500ms -> terminal error`。
2. `useNotesWorkspaceStore.ts` 接入 retry policy，失败后进入 `retrying`，恢复成功记录 `recovered`，耗尽后回落 `error`。
3. `notes-notes` 正式依赖 `@sdkwork/notes-observability` 类型边界。
4. 新增 `workspace-save-retry-policy.contract.test.mjs`，并通过 package/root typecheck 进入主门禁。

## 3. 用户与架构收益

1. 网络短暂波动时，保存会自动退避重试，而不是立即终态失败。
2. 自动重试不再无限持续，最终失败可回到显式 `error`，保留人工重试入口。
3. 保存链已有最小 telemetry 接口，后续观测平台可以直接接入，不必回收当前 store/save 主链。

## 4. 验证结果

```powershell
node .\scripts\workspace-save-retry-policy.contract.test.mjs
node .\scripts\workspace-save-queue.contract.test.mjs
node .\scripts\workspace-save-feedback.contract.test.mjs
node .\scripts\workspace-save-flush-boundary.contract.test.mjs
node .\scripts\workspace-high-risk-flush-boundary.contract.test.mjs
node .\scripts\package-scripts-contract.test.mjs
pnpm.cmd --filter @sdkwork/notes-notes typecheck
pnpm.cmd typecheck
```

## 5. 当前状态

1. `自动退避 / 重试上限 / 保存观测接口`：`L4`
2. `Step 05` 总体：`L3`
3. 下一轮仍应继续停留在 `Step 05`

## 6. 后续缺口

1. 页面关闭、异常退出、崩溃恢复证据矩阵
2. Step 06 本地草稿恢复接入约束
3. 真实 observability 平台实现

