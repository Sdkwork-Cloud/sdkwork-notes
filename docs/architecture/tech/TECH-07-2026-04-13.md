> Migrated from `docs/step/07-索引文档模型冻结-2026-04-13.md` on 2026-06-24.
> Owner: SDKWork maintainers

# Step 07 索引文档模型冻结

- 日期: `2026-04-13`
- 轮次: `Wave-B / 第三十五轮推进`
- 关联能力: `CP07-1 / 索引文档模型冻结`

## 目标

先冻结统一检索最小 contract，确认搜索索引文档、查询参数和命中结果三者已经具备可执行边界，为后续查询 API 和 UI 接入提供稳定基础。

## 本次改动

### 合同测试

- `sdkwork-notes-pc-react/scripts/workspace-search-schema.contract.test.mjs`

### 能力实现

- `sdkwork-notes-pc-react/packages/sdkwork-notes-search/src/index.ts`

### 门禁接入

- `sdkwork-notes-pc-react/package.json`
- `sdkwork-notes-pc-react/scripts/package-scripts-contract.test.mjs`

## 冻结结果

### 统一 schema

1. `NotesSearchDocument`
2. `NotesSearchQuery`
3. `NotesSearchResult`

### 统一 builder

1. `buildNotesSearchDocuments(...)` 统一把远端 note summary、本地 draft、folder snapshot 合成为搜索文档。
2. 本地 draft 内容会覆盖远端 summary 的标题、正文、标签、收藏态和类型。
3. folder path 会被解析为稳定的层级路径。

### 统一结果 envelope

1. `workspace-search` 与 `command-palette` 共享 `NotesSearchResult`
2. 差异仅保留在 `source`

## 当前状态

1. `CP07-1 = L4`
2. `CP07-2 / 查询 API = pending`
3. `CP07-3 / 顶部搜索与命令面板接入 = pending`
4. `CP07-4 / 性能与验证基线 = pending`
5. `Step 07` 暂不宣称 `L4`

## 验证命令

```powershell
node --test --experimental-test-isolation=none scripts/workspace-search-schema.contract.test.mjs
node --test --experimental-test-isolation=none scripts/package-scripts-contract.test.mjs
pnpm.cmd --filter @sdkwork/notes-search typecheck
pnpm.cmd typecheck
```

