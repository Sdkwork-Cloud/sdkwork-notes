> Migrated from `docs/review/step-04-读策略注册表收敛-2026-04-07.md` on 2026-06-24.
> Owner: SDKWork maintainers

# Step 04 读策略注册表收敛评审

- 日期：2026-04-07
- Step：04
- 评审主题：repository 未来读策略注册表与按 key 选策略能力收敛
- 当前成熟度：`L3`

## 1. 评审目标

为 `noteRepository.ts` 建立真实可用的“策略注册 -> key 解析 -> 默认回退”边界，避免工作区读取路径继续被硬编码为单一路径，同时为后续 `read-through-cache / replica-snapshot / queued-sync-snapshot` 演进预留稳定入口。

## 2. 本轮变更

### 2.1 新增读策略注册表模块

- 新增 `sdkwork-notes-pc-react/packages/sdkwork-notes-notes/src/repository/noteWorkspaceReadStrategyRegistry.ts`
- 建立 `createNoteWorkspaceReadStrategyRegistry()` 工厂
- 建立 `defaultKey / listKeys() / resolve()` 统一边界
- 注册表在创建阶段主动拒绝重复 key，并在默认 key 不存在时显式失败

### 2.2 repository 接入按 key 选策略能力

- `AppSdkNoteRepositoryOptions` 新增：
  - `workspaceReadStrategies`
  - `workspaceReadStrategyKey`
- `AppSdkNoteRepository` 构造阶段不再只接受单一默认策略，而是先创建注册表，再按 `workspaceReadStrategyKey` 解析实际使用的策略
- 现有 `workspaceReadStrategy` 注入方式保持兼容，默认行为仍然是 `workspace-snapshot`

### 2.3 工作区读策略键域显式化

- `sdkwork-notes-pc-react/packages/sdkwork-notes-notes/src/types/notesWorkspace.ts` 新增 `NoteWorkspaceReadStrategyKey`
- 当前显式键域包括：
  - `workspace-snapshot`
  - `read-through-cache`
  - `replica-snapshot`
  - `queued-sync-snapshot`
- 这一步把“未来可能会有更多策略”从口头约定提升为代码层面的稳定类型边界

## 3. 契约与验证

### 3.1 新增契约

- `sdkwork-notes-pc-react/scripts/workspace-read-strategy-registry.contract.test.mjs`

覆盖重点：

1. 注册表可以解析已注册的目标策略 key
2. 未注册的未来策略 key 会稳定回退到默认策略
3. 重复 key 与未知默认 key 会被拒绝
4. `createNoteRepository()` 会真实消费 `workspaceReadStrategyKey` 而不是继续硬编码默认策略

### 3.2 总门禁补齐

- `sdkwork-notes-pc-react/package.json`
- `sdkwork-notes-pc-react/scripts/package-scripts-contract.test.mjs`

均已纳入 `workspace-read-strategy-registry.contract.test.mjs`

### 3.3 已通过验证

- `node --test --experimental-test-isolation=none scripts/workspace-read-strategy-registry.contract.test.mjs`
- `node --test --experimental-test-isolation=none scripts/workspace-read-strategy.contract.test.mjs`
- `node --test --experimental-test-isolation=none scripts/workspace-data-source.contract.test.mjs`
- `node --test --experimental-test-isolation=none scripts/package-scripts-contract.test.mjs`
- `pnpm.cmd --filter @sdkwork/notes-notes typecheck`
- `pnpm.cmd typecheck`

### 3.4 环境限制说明

`noteRepository.test.ts` 已同步补充未来策略 key 选择用例，但当前本机 `vitest` 仍受既有 Vite/Windows `spawn EPERM` 限制，无法作为本轮主验证门禁。该限制不影响 Node 契约、包级 typecheck 与根级 typecheck 的通过结论。

## 4. 质量评估

### 4.1 可扩展性

- 评估结论：显著提升
- 说明：repository 已从“只能注入单一策略实例”演进为“支持未来策略注册与按 key 切换”，后续接入本地副本、读穿缓存或排队同步快照时，不需要再改写 repository 主体结构

### 4.2 边界清晰度

- 评估结论：通过
- 说明：当前边界已经可以明确区分：
  - 默认策略是什么
  - 当前可解析哪些策略 key
  - 当请求 key 不可用时采用什么回退行为

### 4.3 风险控制

- 评估结论：通过
- 说明：策略注册表 contract 已进入总门禁，后续任何人如果删除、绕过或错误拼接策略 key，都会在 Node 合同层被直接拦截

## 5. Step 04 复评

本轮完成后，Step 04 的 repository 未来策略接缝已经建立，剩余主阻塞点收敛为：

1. `NotesWorkspacePage.tsx` 仍保留少量 command palette icon 映射与 header action/layout 装配胶水
2. 页面容器尚未完全收敛为薄适配层，因此暂不具备 `L4` 结论

## 6. 结论

本轮增量完成了 Step 04 从“已有读策略边界”到“已有未来策略注册表与按 key 切换能力”的关键演进，repository 的扩展点已从概念性预留变成可验证的真实能力。尽管如此，Step 04 仍保持 `L3`，下一轮应集中清理页面容器剩余胶水，再评估是否具备进入 `L4` 的条件。

