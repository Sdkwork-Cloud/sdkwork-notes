> Migrated from `docs/review/step-04-移动笔记写路径编排收敛-2026-04-07.md` on 2026-06-24.
> Owner: SDKWork maintainers

# Step 04-移动笔记写路径编排收敛

- 日期：`2026-04-07`
- 轮次：`Wave-B / 第十五轮推进`
- 当前 Step：`04-工作区边界收敛与数据访问抽象`
- 当前等级：`L3`
- 本轮结论：`写路径主残留已进一步收口，但 Step 04 仍未达到 L4`

## 1. 本轮目标

在完成 `renameFolder` 下沉后，`moveNote` 成为 Step 04 中最后一条最明显的 store 内联写路径。本轮目标是继续按同一模式，将 `moveNote()` 的远程调用和状态计划拼装下沉到 write coordinator。

## 2. 实施结果

本轮已完成以下落地：

1. 在 `packages/sdkwork-notes-notes/src/services/noteWorkspaceWriteCoordinator.ts` 中新增 `moveNote` 依赖、`NoteWorkspaceMoveNoteStateResult` 和 `moveNoteState()`。
2. `moveNoteState()` 统一承担 `planMovedNoteState()` 预判、`workspaceService.moveNote()` 调用、错误归一化和返回计划透出。
3. 在 `packages/sdkwork-notes-notes/src/store/useNotesWorkspaceStore.ts` 中，将 `moveNote()` 改为消费 write coordinator，store 不再直接组合 service 调用与 move plan。
4. 在 `sdkwork-notes-pc-react/scripts/workspace-write-path.contract.test.mjs` 中新增 `moveNoteState()` contract，并先验证红灯失败原因为 `coordinator.moveNoteState is not a function`，随后完成实现转绿。

## 3. 验证结果

本轮验证通过：

```powershell
node --test --experimental-test-isolation=none scripts/workspace-write-path.contract.test.mjs
pnpm.cmd --filter @sdkwork/notes-notes typecheck
pnpm.cmd typecheck
```

验证结论：

1. `workspace-write-path.contract.test.mjs` 已覆盖 `moveNoteState()` 的真实协作链。
2. 包级和根级 typecheck 均通过，说明下沉实现已被工作区门禁接纳。

补充说明：

- 包内 Vitest 在当前环境依旧受 `spawn EPERM` 约束，不作为本轮结论依据。

## 4. 评估标准与结论

### 4.1 边界收敛评估

- 评估标准：store 是否退出 `moveNote` 的远程编排职责。
- 本轮结果：达到。Store 已只消费 coordinator 结果。

### 4.2 正确性评估

- 评估标准：移动后是否同步维护 note parentId、activeNote、expandedFolderIds 和 nextParentId。
- 本轮结果：已由 Node contract 明确锁定。

### 4.3 工程门禁评估

- 评估标准：是否保留了红绿验证证据，并继续通过工作区 typecheck 门禁。
- 本轮结果：达到。

### 4.4 Step 完成度评估

- 评估标准：是否已经清空 Step 04 的主要写路径残留，并进入退出收口阶段。
- 本轮结果：部分达到。写路径层面已明显收口，但 repository 策略接缝与页面容器胶水仍未完成，因此 Step 04 仍保持 `L3`。

## 5. 审计结论

本轮之后可以确认：

1. write coordinator 已覆盖 `createNote / createFolder / renameFolder / moveNote` 四类主写路径。
2. Step 04 的主要残留已从“store 内联写路径编排”进一步转移到“repository 演进接缝 + 页面容器装配纯化”。
3. 当前最准确状态仍然是 `Step 04 / L3`，但其阻塞已经更加集中、更加可收口。

## 6. 剩余缺口

当前需要继续收口的核心问题：

1. repository 仅完成能力显式化，尚未形成 `read-through / replica / queued-sync` 的策略接缝。
2. 页面容器层仍保留少量 icon / layout / status 胶水，尚未完全退化为纯装配层。

## 7. 下一轮建议

建议下一轮继续留在 Step 04，并按以下顺序推进：

1. 优先处理 repository 的未来同步策略接缝设计。
2. 然后继续压缩 `NotesWorkspacePage.tsx` 中残留的页面装配胶水。
3. 只有在上述两项都获得可验证证据后，才重新评估 Step 04 是否具备 `L4` 退出条件。

