# 99. Step 极速并行执行总作战手册

## 1. 文档定位

本文件不是替代 `94 / 96 / 98`，而是把三者收敛成一份可以直接执行的“总作战手册”。

目标只有两个：

- 在风险可控前提下，以最快速度完成 `00-13`
- 确保每个 Step 执行完毕后，确实兑现其对应的架构能力，而不是只完成局部代码改动

## 2. 核心原则

极速推进并不意味着无序并行，而是坚持以下公式：

`主脊柱串行冻结 + 波次内主写入范围并行 + 独立验证车道 + 波次级总验收`

如果缺少其中任意一项，速度越快，返工越大。

## 3. 先决条件

在按本手册执行前，以下文档必须已经可用：

- `docs/step/README.md`
- `docs/step/90-架构能力-Step-代码目录-证据映射矩阵.md`
- `docs/step/94-Step并行执行编排与车道拆分建议.md`
- `docs/step/95-架构能力闭环验收标准.md`
- `docs/step/96-Step并行执行周计划与排班建议.md`
- `docs/step/98-Step并行车道交付包与集成交接标准.md`

## 4. Step 完成即能力完整实现的判定公式

任何一个 Step，只有同时满足以下五项，才允许判定为“该 Step 已完整实现对应架构能力”：

1. 该 Step 文档 `8.6` 中列出的能力切片已兑现
2. 该 Step 文档 `8.7` 中列出的最速执行顺序已完成收口
3. `95` 的闭环标准达到 `L4`
4. `97` 的架构回写和能力兑现清单已输出
5. 该 Step 对下一 Step 的准入条件已冻结

只满足 1-2 项，不允许视为完成。

## 5. 当前仓库默认推荐模式

对当前 `sdkwork-notes` 仓库，推荐默认使用：

`Fast-M`

原因：

- 相比 `Fast-S`，推进速度更快
- 相比 `Fast-L`，共享边界冲突更少
- 能同时覆盖主业务链、平台链和质量链
- 对当前 Notes 应用的包结构最匹配

## 6. 三档极速执行模式

### 6.1 `Fast-S`

适用：

- `2` 人或 `2` 个子 Agent
- 以稳定性优先，适合关键基线冻结阶段

编组：

- `Lane-S1`：主脊柱 / 集成 / 文档 / 验收
- `Lane-S2`：功能实现

适合波次：

- `Wave-A`
- `Wave-B` 前半段

### 6.2 `Fast-M`

适用：

- `4` 人或 `4` 个子 Agent
- 当前仓库的默认推荐模式

编组：

- `Lane-M1`：集成 / 审计 / 文档 / 最终收口
- `Lane-M2`：壳层 / 安全 / 配置治理
- `Lane-M3`：工作区初始化 / 编辑器 / 本地存储
- `Lane-M4`：搜索 / 同步 / 测试门禁

适合波次：

- `Wave-A`
- `Wave-B`
- `Wave-C`

### 6.3 `Fast-L`

适用：

- `6` 人或 `6` 个子 Agent
- 有成熟集成 Owner，且周中可多次集成

编组：

- `Lane-L1`：集成 / 架构 / 文档 / 发布结论
- `Lane-L2`：壳层 / 安全 / 配置治理
- `Lane-L3`：初始化 / 编辑器 / 本地存储
- `Lane-L4`：搜索 / 同步
- `Lane-L5`：桌面壳 / 发布链
- `Lane-L6`：测试 / 性能 / 版本恢复

适合波次：

- `Wave-C`
- `Wave-D`

## 7. 推荐的最快全局执行顺序

### 7.1 主脊柱

必须严格按以下顺序冻结：

1. `00`
2. `01`
3. `02`
4. `03`

说明：

- `00-03` 是整个项目的主脊柱
- 不允许跳过或并行越级

### 7.2 波次推进

按最快且可控的顺序，建议：

1. `Wave-A`
2. `Wave-B`
3. `Wave-C`
4. `Wave-D`

每个波次内部再按车道拆并行。

## 8. 波次级极速作战蓝图

### 8.1 `Wave-A` 作战蓝图

目标：

- 冻结执行门禁、差距矩阵、共享骨架、安全治理

推荐模式：

- `Fast-M`

推荐车道：

- `A1`：审计与文档
- `A2`：共享壳层与包骨架
- `A3`：会话安全与配置治理
- `A4`：验证与 contract test

放行条件：

- `01` 差距矩阵已冻结
- 共享目录边界和安全治理边界已冻结

收口条件：

- `Wave-A` 总验收通过

### 8.2 `Wave-B` 作战蓝图

目标：

- 打通工作区初始化、编辑器可靠性、本地存储与恢复链

推荐模式：

- `Fast-M`

推荐车道：

- `B1`：初始化编排
- `B2`：编辑器与保存可靠性
- `B3`：本地存储与恢复链
- `B4`：集成回归与验证

放行条件：

- `04` 已冻结 `repository / orchestrator / selector` 边界
- `05` 保存状态机已冻结
- `06` schema 与权威源已冻结

收口条件：

- 搜索和同步已具备稳定本地数据入口

### 8.3 `Wave-C` 作战蓝图

目标：

- 完成搜索、同步、桌面壳与可信发布链一期

推荐模式：

- `Fast-M` 或 `Fast-L`

推荐车道：

- `C1`：索引与统一查询
- `C2`：同步队列与冲突恢复
- `C3`：桌面 runtime 与发布链
- `C4`：搜索/同步/桌面专项验证

放行条件：

- `07` 索引 schema 冻结
- `08` 同步状态机冻结
- `09` capability 与 updater 落点冻结

收口条件：

- 搜索、同步、桌面三条能力链都具备可验证一期闭环

### 8.4 `Wave-D` 作战蓝图

目标：

- 完成质量门禁、性能治理、版本恢复、发布收口

推荐模式：

- `Fast-L`

推荐车道：

- `D1`：质量门禁
- `D2`：性能治理
- `D3`：版本历史与恢复
- `D4`：发布候选与架构回写

放行条件：

- `10` 已可稳定运行高层自动化门禁
- `11` 性能基线和数据集已冻结
- `12` revision 模型已冻结

收口条件：

- 发布 go/no-go 已正式形成

## 9. 每轮并行执行固定算法

推荐每个波次、每个大 Step 都按下列算法运行：

1. 冻结本轮目标、边界、Owner、共享文件
2. 冻结统一验证命令与检查点
3. 拆车道并分配主写入目录
4. 周中或中期做一次集成检查
5. 统一执行验证车道
6. 输出审计、复盘、回写
7. 通过后再进入下一轮

这是本项目的默认执行算法，不建议跳步。

## 10. 每个 Step 最快完成的收口动作

每个 Step 的最快正确收口方式，固定如下：

1. 完成主目标实现
2. 对照 Step `8.6` 检查能力是否真正兑现
3. 对照 Step `8.7` 检查执行顺序是否完整闭环
4. 运行适用测试与验证
5. 输出 `docs/review/` 审计记录
6. 回写 `docs/架构/`
7. 冻结下一 Step 输入

任何缺少第 2、5、6、7 步的收口，都视为不完整。

## 11. 不允许并行的情形

以下情况不允许继续并行推进：

- 主写入目录和 Owner 未冻结
- 共享边界文件无单一拍板人
- 统一验证命令不存在
- 当前波次仍有 `B3/B4` 级阻塞
- 上一波次总验收未通过

## 12. 最容易失败的四类反模式

- 只并行实现，不并行验证
- 先写代码，后补边界
- 一个车道同时主写多个高风险共享文件
- Step 自称完成，但没有兑现其 `8.6` 能力切片

## 13. 推荐的实际落地节奏

若按默认推荐模式 `Fast-M` 执行，建议采用：

- 周一：冻结输入、Owner、边界、检查点
- 周二到周三：并行实现
- 周三晚：中期集成
- 周四：补测试、补验证、修冲突
- 周五：审计、回写、波次或 Step 收口

## 14. Step 作战总表

下表用于在真正执行时快速回答六个问题：

- 当前 Step 对应哪个能力切片
- 当前 Step 直接对齐哪些架构章节
- 该 Step 最推荐的执行模式是什么
- 该 Step 最适合拆成哪些车道
- 该 Step 结束时必须拿到哪些关键证据
- 什么情况属于假完成，必须拦截

| Step | 对应能力 | 对齐架构章节 | 推荐模式 | 推荐主车道 | 结束时必须拿到的关键证据 | 假完成拦截信号 |
| --- | --- | --- | --- | --- | --- | --- |
| `00` | 执行治理基线 | `09` `10` | `Fast-S` | 门禁/证据/回滚 | 执行门禁、检查点、证据与回滚规则冻结 | 后续 Step 仍各写各的完成标准 |
| `01` | 基线审计与差距矩阵 | `README` `02` `03` `07` `08` `10` | `Fast-S` | 盘点/差距矩阵/门禁审计 | 现状清单、差距矩阵、风险优先级、Step 映射 | 只写主观判断，没有真实代码和脚本盘点 |
| `02` | `CAP-01` | `02` `03` `04` `09` | `Fast-M` | 壳层/包骨架/contract test | 壳层职责矩阵、目录落点、依赖方向、contract test | 新能力仍默认堆进 `notes-notes` |
| `03` | `CAP-02` | `04` `05` `06` `08` | `Fast-M` | 安全存储/配置治理/hydration | 安全存储迁移方案、配置边界、回归测试 | 高敏会话仍长期留在高风险持久层 |
| `04` | `CAP-03` | `05` `06` `07` | `Fast-M` | repository/orchestrator/selector | 初始化时序图、边界表、集成测试、首屏验证 | 本地/搜索/同步仍需重写页面才能接入 |
| `05` | `CAP-04` | `06` `07` | `Fast-M` | 编辑事件/保存编排/失败恢复 | 保存状态机、失败恢复测试、切换 flush 验证 | 断网/崩溃/切换场景仍易丢稿 |
| `06` | `CAP-05` | `05` `07` `09` | `Fast-M` | schema/适配器/恢复链 | schema、迁移策略、恢复链验证、离线冒烟 | 本地层仍只是临时缓存，没有权威源定义 |
| `07` | `CAP-06` | `04` `07` `09` | `Fast-M` | 索引/查询/UI 接入 | 索引 schema、统一查询服务、性能基线 | 搜索和命令面板仍维护两套语义 |
| `08` | `CAP-07` | `05` `07` `10` | `Fast-M` | 状态机/队列/UI 与恢复 | 同步状态机、冲突演练、离在线切换验证 | 失败仍只能中断或静默覆盖 |
| `09` | `CAP-08` | `06` `08` | `Fast-M` 或 `Fast-L` | runtime/workflow/capability | dry-run、签名/校验、capability 审查记录 | 只能打包，不能证明可信交付 |
| `10` | `CAP-09` | `08` `09` `10` | `Fast-M` 或横向常驻车道 | E2E/视觉/性能安全/CI | 测试矩阵、CI 门禁、主路径自动化结果 | 发布前仍主要依赖人工验收 |
| `11` | `CAP-10` | `07` `09` `10` | `Fast-L` | 初始化性能/虚拟化/编辑与检索优化 | 性能基线、回归脚本、10k+ 数据集对比 | 优化很多，但没有量化收益证明 |
| `12` | `CAP-11` | `05` `07` `09` `10` | `Fast-L` | revision/恢复 UI/恢复验证 | revision 模型、恢复演练、兼容性测试 | 只有后台历史数据，用户不能稳定恢复 |
| `13` | `CAP-12` | `README` `09` `10` | `Fast-S` 或 `Fast-M` | 证据汇总/能力兑现/发布结论 | go/no-go、能力兑现清单、下一轮输入 | 无法区分“已闭环能力”和“目标态能力” |

### 14.1 Step 放行三层门

任何 Step 要想正式放行到下一步，都必须同时通过以下三层门：

1. 功能门
   - 主实现完成
   - 检查点通过
2. 能力门
   - `8.6` 能力切片兑现
   - `95` 闭环等级达到 `L4`
3. 文档门
   - `97` 要求的回写、审计、兑现清单齐全

只过功能门，不过能力门或文档门，统一视为假完成。

## 15. 极速调度顺序建议

若目标是“最快且完整”跑完全部 Step，推荐使用以下调度顺序：

### 15.1 第一阶段

- `00`
- `01`

执行目标：

- 先冻结规则和基线，避免后续快跑建立在错误假设上

### 15.2 第二阶段

- `02`
- `03`
- `10` 预埋启动

执行目标：

- 骨架和安全是后续所有业务能力的共用底座
- `10` 需要尽早铺设，不能等到后期再补

### 15.3 第三阶段

- `04`
- `05`
- `06`

执行目标：

- 用初始化、保存、本地层打通 Notes 的真实主链路

### 15.4 第四阶段

- `07`
- `08`
- `09`

执行目标：

- 搜索、同步、桌面交付并行形成产品能力跃迁

### 15.5 第五阶段

- `11`
- `12`
- `13`

执行目标：

- 用性能、历史恢复和最终发布判断完成总收口

## 16. 最快并行执行的硬规则

为了保证“快而不乱”，实际执行时必须再额外遵守以下规则：

- 一个车道只允许拥有一组主写入目录
- 一个共享边界文件只能有一个最终拍板 Owner
- `10` 不能滞后于 `04-09` 的主要实现
- 每一轮中期集成只处理冲突、偏移和验证滞后，不临时扩 scope
- 每个 Step 收口前必须同时经过 `91 + 95 + 97`
- 每个波次收口前必须经过 `93`

## 17. 结论

对当前 Notes 应用，最快且完整的执行方式不是“把 00-13 全部分散并行”，而是：

- 用 `00-03` 冻结主脊柱
- 用 `Fast-M` 作为默认并行模式
- 在 `Wave-C` 和 `Wave-D` 需要提速时，升级到 `Fast-L`
- 用 `95 + 97 + 99` 三份规则确保每个 Step 收口时真正兑现架构能力

如果严格按本手册推进，才能在“快”和“完整”之间取得真实平衡，而不是用短期速度换长期返工。
