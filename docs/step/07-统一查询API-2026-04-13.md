# Step 07 统一查询 API

- 日期: `2026-04-13`
- 轮次: `Wave-B / 第三十六轮推进`
- 关联能力: `CP07-2 / 统一查询 API`

## 目标

在 `CP07-1` 已冻结 schema 的前提下，补齐统一查询函数与最小 search service，确认统一检索已经具备真正可执行的过滤、排序和结果输出能力。

## 本次改动

### 合同测试

- `sdkwork-notes-pc-react/scripts/workspace-search-query.contract.test.mjs`

### 能力实现

- `sdkwork-notes-pc-react/packages/sdkwork-notes-search/src/index.ts`

### 门禁接入

- `sdkwork-notes-pc-react/package.json`
- `sdkwork-notes-pc-react/scripts/package-scripts-contract.test.mjs`

## 查询能力

### 统一查询函数

1. `searchNotesSearchDocuments(...)`
2. 支持 `text / tags / folderId / includeTrashed / limit`
3. 返回统一 `NotesSearchResult[]`

### 统一 service

1. `createInMemoryNotesSearchService(...)`
2. `search(...)`
3. `rebuild(...)`

### 最小排序规则

1. 标题命中优先于正文命中
2. 收藏项轻量加权
3. 非 trash 优先于 trash
4. 同分按 `updatedAt` 排序

## 当前状态

1. `CP07-1 = L4`
2. `CP07-2 = L4`
3. `CP07-3 / 顶部搜索与命令面板接入 = pending`
4. `CP07-4 / 性能与验证基线 = pending`
5. `Step 07` 暂不宣称 `L4`

## 验证命令

```powershell
node --test --experimental-test-isolation=none scripts/workspace-search-schema.contract.test.mjs
node --test --experimental-test-isolation=none scripts/workspace-search-query.contract.test.mjs
node --test --experimental-test-isolation=none scripts/package-scripts-contract.test.mjs
pnpm.cmd --filter @sdkwork/notes-search typecheck
pnpm.cmd typecheck
```
