# Step 08 / CP08-4 / 工作区同步阻塞问题恢复动作语义与受影响笔记定位 - 2026-04-14

## 本轮结论

- `Step 08 = L2`
- `CP08-4 / 冲突与失败恢复验证 = L2`
- `CP08-4 / 工作区同步阻塞问题恢复动作语义与受影响笔记定位 = L3`

## 本轮完成

- `sdkwork-notes-pc-react/packages/sdkwork-notes-notes/src/services/noteWorkspaceSelectors.ts`
  - `NotesWorkspaceSyncTaskLike` 现在显式接受 `entityId`。
  - `NotesWorkspaceSyncSummary` 现在新增：
    - `primaryEntityId`
    - `primaryMessage`
  - `syncSummary` 现在会把主阻塞任务对应的受影响笔记身份与可读问题消息一起导出。
- `sdkwork-notes-pc-react/packages/sdkwork-notes-notes/src/services/noteWorkspacePagePresentationModel.ts`
  - `syncCard` 现在新增：
    - `actionKind: 'retry-sync' | 'review-note' | null`
    - `actionTargetNoteId`
  - “最新问题”明细现在优先展示 `primaryMessage`，而不是只展示原始问题码。
  - 当前 action 优先级已冻结为：
    - 若主状态为 `failed / conflict` 且存在 `primaryEntityId`，则 action 为 `review-note`
    - 否则若仍有 `pending` 队列，则 action 为 `retry-sync`
- `sdkwork-notes-pc-react/packages/sdkwork-notes-notes/src/pages/NotesWorkspacePage.tsx`
  - 同步卡片 CTA 现在会按 `actionKind` 正确分流：
    - `retry-sync -> requestSyncDrain()`
    - `review-note -> selectNote(actionTargetNoteId)`
- 国际化资源：
  - `sdkwork-notes-pc-react/packages/sdkwork-notes-i18n/src/resources/en-US.ts`
  - `sdkwork-notes-pc-react/packages/sdkwork-notes-i18n/src/resources/zh-CN.ts`
  - 已新增 `notes.actions.reviewSyncIssue`
- 合同测试已补齐：
  - `sdkwork-notes-pc-react/scripts/workspace-view-model.contract.test.mjs`
  - `sdkwork-notes-pc-react/scripts/workspace-page-presentation-model.contract.test.mjs`
  - `sdkwork-notes-pc-react/scripts/workspace-page-container-boundary.contract.test.mjs`

## 当前用户可见变化

- 当主阻塞同步问题可以定位到具体笔记时，工作区同步卡片不再继续展示误导性的“重试同步”，而是切换为“查看受影响笔记”。
- 当当前没有可直接定位的阻塞笔记，但队列里仍有 `pending` 任务时，页面继续保留“重试同步”入口，用于请求已有 runtime 继续 drain。
- “最新问题”现在优先展示更可读的失败或冲突消息，而不是只暴露原始错误码。
- 页面层不再把所有同步问题都收敛成一个 drain 动作，用户可以更快进入受影响笔记做人工处理。

## 为什么这轮仍然只算 L3

- 这轮交付的是“恢复动作语义更诚实、受影响笔记可定位”，不是“真实远端恢复闭环”。
- `review-note` 的真实语义只是“打开受影响笔记作为人工恢复入口”，它不会：
  - 解决远端冲突
  - 重写 `failed / conflict` 任务状态
  - 合并 `remoteCursor`
- `requestSyncDrain()` 仍然只会请求当前 runtime 继续 drain 已可执行队列；它不会让 `failed / conflict` 任务 magically 变成已恢复。
- 当前仍未完成：
  - 真实 `remoteApply`
  - ack apply / `remoteCursor` 合并闭环
  - 真实 conflict recovery UI
  - 离线/在线切换 smoke

## 验证基线

```powershell
node --test --experimental-test-isolation=none scripts/workspace-view-model.contract.test.mjs
node --test --experimental-test-isolation=none scripts/workspace-page-presentation-model.contract.test.mjs
node --test --experimental-test-isolation=none scripts/workspace-page-container-boundary.contract.test.mjs
pnpm.cmd test:workspace:contracts
pnpm.cmd typecheck
```

## 下一轮入口

- 若继续停留在 `Step 08 / CP08-4`，优先补真实 `remoteApply` 闭环，或把 `review-note` 继续推进为真正的冲突恢复交互，而不是继续堆更多展示包装。
- 在真实远端闭环出现前，必须继续保持：
  - `Step 08 = L2`
  - `CP08-4 / 冲突与失败恢复验证 = L2`
