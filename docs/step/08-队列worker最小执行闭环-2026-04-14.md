# Step 08 - 队列 worker 最小执行闭环 - 2026-04-14

## 本轮目标

- 从 `CP08-3 / 主写入路径接入` 正式切换到 `CP08-4 / 冲突与失败恢复验证`。
- 在 `@sdkwork/notes-sync` 包内交付最小可验证的 worker 执行链，而不是提前扩散到 UI、desktop runtime 或远端 transport。

## 本轮非目标

- 不在本轮接入真实远端写入 SDK。
- 不在本轮交付冲突提示 UI、人工恢复交互或离在线切换 smoke。
- 不在本轮改写 `notes-notes` 的主写入链路。

## 实际落地

1. `sdkwork-notes-pc-react/scripts/workspace-sync-worker.contract.test.mjs`
   - 新增 5 条 Node contract，冻结以下语义：
   - oldest queued task 必须先持久化为 `running` 再执行。
   - 到期 `retrying` 任务必须先释放回 `queued` 再自动重放。
   - handler 结果必须回写为 `completed / conflict / retrying / failed`。
   - 成功回执必须能够刷新 `remoteCursor`。
2. `sdkwork-notes-pc-react/packages/sdkwork-notes-sync/src/index.ts`
   - 新增 `executeNextNotesSyncTask(...)` 最小 executor API。
   - executor 会先释放 `nextRetryAt <= execution.at` 的任务，再按 `enqueuedAt` 选择 oldest runnable queued task。
   - executor 会在调用注入 handler 前先回写 `running`，并在 handler 返回后回写最终状态。
3. `sdkwork-notes-pc-react/package.json`
   - `test:workspace:contracts` 已接入 `workspace-sync-worker.contract.test.mjs`，不再依赖人工单独执行。
4. `sdkwork-notes-pc-react/scripts/package-scripts-contract.test.mjs`
   - 已同步冻结新的根级合同脚本编排。

## 验证基线

```powershell
node --test --experimental-test-isolation=none scripts/workspace-sync-worker.contract.test.mjs
node --test --experimental-test-isolation=none scripts/workspace-sync-state-machine.contract.test.mjs
node --test --experimental-test-isolation=none scripts/workspace-sync-queue.contract.test.mjs
node --test --experimental-test-isolation=none scripts/workspace-sync-write-path.contract.test.mjs
node --test --experimental-test-isolation=none scripts/package-scripts-contract.test.mjs
pnpm.cmd test:workspace:contracts
pnpm.cmd --filter @sdkwork/notes-sync typecheck
pnpm.cmd typecheck
```

## 当前等级判断

- `Step 08`：`L2`
- `CP08-4 / 冲突与失败恢复验证`：`L2`
- `CP08-4 / notes-sync worker 最小执行闭环`：`L3`

## 仍缺失项

- 缺少真实远端 handler 与回执应用链路。
- 缺少 `notes-notes` 或 desktop runtime 对 executor 的调度接入。
- 缺少冲突提示、人工恢复与手动 replay 入口。
- 缺少离在线切换、断网恢复与冲突演练证据。

## 下一轮最优入口

- 继续停留在 `Step 08 / CP08-4`。
- 优先把 `executeNextNotesSyncTask(...)` 接到真实运行时 handler 和持久化调度边界，再补远端回执应用与冲突恢复 UI。
