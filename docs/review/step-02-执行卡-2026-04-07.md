# Step 02 执行卡

- 日期：`2026-04-07`
- Step：`02-共享壳层与工程骨架收敛`
- 波次：`Wave-A / Step 02 收口波次`
- 当前结论：`L4`
- 执行目标：把 Step 02 从“骨架已落地但验证未闭环”的中间态，推进到“工程骨架、脚本门禁、锁文件、文档回写全部闭环”的可审计完成态。

## 1. 本轮输入

- 已存在五个未来能力包骨架：
  - `packages/sdkwork-notes-local`
  - `packages/sdkwork-notes-search`
  - `packages/sdkwork-notes-sync`
  - `packages/sdkwork-notes-observability`
  - `packages/sdkwork-notes-updater`
- 已存在壳层边界清单：
  - `packages/sdkwork-notes-shell/src/application/packageBoundaries.ts`
- 已存在 Step 02 的初版 review / 架构 / release 回写，但结论仍停留在 `L3`。

## 2. 本轮实际实施

- 修复 root package 直接在 PowerShell 下执行 `node --test` 时触发 `spawn EPERM` 的验证入口问题。
- 修复 turbo dry-run 合同测试依赖管道抓取 stdout 时在当前环境下失败的问题。
- 通过 `pnpm.cmd install --lockfile-only --ignore-scripts` 收敛 lockfile，把五个新包 importer 正式写入 `pnpm-lock.yaml`。
- 重新执行 workspace contract、desktop contract、root typecheck，并把 Step 02 升级为 `L4`。

## 3. 代码与脚本变更

- `sdkwork-notes-pc-react/package.json`
  - `test:workspace:contracts` 切换为 `node --test --experimental-test-isolation=none`
  - `test:desktop:contracts` 切换为 `node --test --experimental-test-isolation=none`
- `sdkwork-notes-pc-react/scripts/package-scripts-contract.test.mjs`
  - 新增脚本契约，要求 workspace / desktop 合同链统一使用单进程 Node test 入口
- `sdkwork-notes-pc-react/scripts/internal-packages-turbo-contract.test.mjs`
  - 将 turbo dry-run JSON 抓取方式从管道 stdout 改为文件描述符落盘再读取
- `sdkwork-notes-pc-react/pnpm-lock.yaml`
  - 写入五个未来能力包 importer，消除 turbo 对 lockfile transitive closure 的缺口告警

## 4. 验证记录

- 失败复现：
  - `node --test scripts/workspace-boundary-contract.test.mjs`
  - `pnpm.cmd test:workspace:contracts`
  - 现象：`spawn EPERM`
- 红灯验证：
  - `node --test --experimental-test-isolation=none scripts/package-scripts-contract.test.mjs`
  - 结果：在 `package.json` 未更新前按预期失败
- 绿灯验证：
  - `node --test --experimental-test-isolation=none scripts/package-scripts-contract.test.mjs`
  - `node --test --experimental-test-isolation=none scripts/internal-packages-turbo-contract.test.mjs`
  - `pnpm.cmd test:workspace:contracts`
  - `pnpm.cmd test:desktop:contracts`
  - `pnpm.cmd typecheck`
  - `pnpm.cmd install --lockfile-only --ignore-scripts`
  - `rg -n "^  packages/sdkwork-notes-(local|search|sync|observability|updater):" pnpm-lock.yaml`
- 验证结论：
  - workspace contract 绿灯
  - desktop contract 绿灯
  - root typecheck 绿灯
  - 五个未来能力包 importer 已进入 lockfile

## 5. 闭环判断

- 设计闭环：完成
- 实现闭环：完成
- 测试闭环：完成
- 验证闭环：完成
- 文档闭环：完成
- 集成闭环：完成

## 6. 当前等级与下一步

- 当前 Step 等级：`L4`
- 是否允许进入下一 Step：`允许`
- 下一最优入口：`Step 03 - 会话安全、环境配置与运行时治理收口`
- 并行建议：
  - 可并行：Step 03 的现状盘点、环境变量审计、会话存储边界审计
  - 必须串行：认证令牌语义定版、配置权限边界定版、运行时安全门禁定版
