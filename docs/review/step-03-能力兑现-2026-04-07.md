# Step 03 能力兑现清单

- 日期：`2026-04-07`
- Step：`03-会话安全与配置治理升级`
- 当前能力等级：`L4`

## 1. 能力兑现映射

| 能力编号 | 能力说明 | 当前状态 | 证据 |
| --- | --- | --- | --- |
| `CAP-03-A` | `notes-core` 具备统一会话存储适配接口 | 已兑现 | `configureAppSdkSessionStoreAdapter` |
| `CAP-03-B` | Web 会话主持久层已迁出 `localStorage` | 已兑现 | `sessionStorage` + 旧键迁移清理逻辑 |
| `CAP-03-C` | Desktop 会话具备原生权威副本与前端镜像桥接 | 已兑现 | `sessionBridge.ts` + `desktop-session.json` |
| `CAP-03-D` | 配置来源优先级、`ownerMode`、平台解析规则已冻结 | 已兑现 | `resolveAppSdkRuntimeContext()` |
| `CAP-03-E` | `.env.*` 提供显式 owner mode 基线 | 已兑现 | `.env.example / .env.development / .env.test / .env.production` |
| `CAP-03-F` | Step 03 合同测试已进入标准脚本链 | 已兑现 | `test:workspace:contracts`、`test:desktop:contracts` |
| `CAP-03-G` | Rust 原生会话读写具备独立测试证明 | 已兑现 | `pnpm.cmd test:desktop:rust` |

## 2. 最终判断

- Step 03 已完成从 `L0/L1 目标态` 到 `L4` 的收口升级。
- 当前不存在阻塞 Step 03 放行的验证、集成或文档缺口。
- 当前允许切换到 Step 04。

## 3. 未在本 Step 内兑现的能力

| 能力编号 | 未兑现原因 | 计划在哪个 Step 继续 |
| --- | --- | --- |
| `CAP-03-X1` | Desktop OS Keychain / Credential Manager 尚未接入 | `Step 09 / Step 11` |
| `CAP-03-X2` | 安全事件、异常会话检测与审计链尚未建立 | `Step 10 / Step 11` |
| `CAP-03-X3` | 高层 E2E、视觉回归与安全专项测试尚未建设 | `Step 10 / Step 11` |

## 4. 变更后的现实约束

- 后续任何认证、登出、刷新或启动期水合逻辑都必须复用 `notes-core` 会话存储适配接口，不能重新散落到页面或包内私有存储逻辑。
- 后续任何运行时配置读取都应优先复用 `resolveAppSdkRuntimeContext()`，不允许页面层继续拼接环境变量优先级。
- Desktop 业务层不得直接读写 `desktop-session.json`，必须经由 Tauri 命令桥。

## 5. 必须同步给后续 Step 的输入

- Step 04 的初始化链设计必须建立在稳定会话与稳定配置边界之上。
- Step 06/07/08 若引入本地能力，不得回退到 `localStorage` 持久化高敏会话。
- Step 09/11 若继续强化安全链，应在现有会话桥和原生状态模型上演进，而不是推翻当前适配层。

## 6. 风险与遗留项

- Web 侧会话仍属于同源脚本可访问存储。
- Desktop 会话当前是原生文件而非系统密钥链。
- 可信发布链、安全事件观测与更强前端安全基线仍未落地。
