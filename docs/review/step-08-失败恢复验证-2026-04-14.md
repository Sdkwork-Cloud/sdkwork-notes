# Step 08 失败恢复验证

- 日期：`2026-04-14`
- 审计对象：`CP08-4 / 冲突与失败恢复验证`
- 本轮范围：`notes-sync worker 最小执行闭环`

## 审计结论

- `CP08-4` 已正式启动，不再停留在“未开始”。
- 本轮新增的 `notes-sync worker 最小执行闭环` 已具备设计、实现、测试、验证与文档证据，可评为 `L3`。
- `CP08-4` 整体仍只能评为 `L2`，因为真实运行时接入、远端回执应用、冲突恢复 UI 与离在线切换验证仍未完成。
- `Step 08` 整体继续维持 `L2`。

## 当前等级判断

- `Step 08`：`L2`
- `CP08-4 / 冲突与失败恢复验证`：`L2`
- `CP08-4 / notes-sync worker 最小执行闭环`：`L3`

## 闭环判定

### `CP08-4 / notes-sync worker 最小执行闭环`

- 设计闭环：是
- 实现闭环：是
- 测试闭环：是
- 验证闭环：是
- 文档闭环：是
- 集成闭环：否

结论：

- 按 `docs/step/95-架构能力闭环验收标准.md` 口径，本子切片已达到“设计、实现、测试、验证完成，但集成不足”的 `L3`。

## 关键证据

- `sdkwork-notes-pc-react/packages/sdkwork-notes-sync/src/index.ts`
  - 新增 `executeNextNotesSyncTask(...)`。
  - 执行顺序已冻结为：
    - 释放到期 `retrying`
    - 选择 oldest `queued`
    - 先回写 `running`
    - 执行注入 handler
    - 回写 `completed / retrying / failed / conflict`
- `sdkwork-notes-pc-react/scripts/workspace-sync-worker.contract.test.mjs`
  - 已覆盖成功回执、自动重放、冲突回写、可重试失败、终态失败五类核心执行路径。
- `sdkwork-notes-pc-react/package.json`
  - `test:workspace:contracts` 已纳入 worker contract。
- `sdkwork-notes-pc-react/scripts/package-scripts-contract.test.mjs`
  - 已冻结新的根级合同脚本编排，避免后续根级验收漏测。

## 本轮验证结果

```powershell
node --test --experimental-test-isolation=none scripts/workspace-sync-worker.contract.test.mjs
node --test --experimental-test-isolation=none scripts/workspace-sync-state-machine.contract.test.mjs
node --test --experimental-test-isolation=none scripts/workspace-sync-queue.contract.test.mjs
node --test --experimental-test-isolation=none scripts/workspace-sync-write-path.contract.test.mjs
node --test --experimental-test-isolation=none scripts/package-scripts-contract.test.mjs
pnpm.cmd test:workspace:contracts
pnpm.cmd --filter @sdkwork/notes-sync typecheck
pnpm.cmd typecheck
```

## 本轮已冻结的执行语义

1. worker 只消费 oldest runnable queued task，不并发拉取多条任务。
2. `retrying` 任务只有在 `nextRetryAt <= execution.at` 时才会被释放回 `queued`。
3. handler 调用前必须先持久化 `running`，避免执行中断时队列状态失真。
4. 成功回执可以刷新 `remoteCursor`，且不会丢失既有 cursor。
5. retryable failure 统一回写为 `retrying`，terminal failure 统一回写为 `failed`。
6. conflict 统一进入 `conflict` 状态，保留手动恢复入口的语义位置。

## 风险与剩余缺口

- 当前 executor 仍是包内 API，不是实际 background worker runtime。
- 当前 handler 仍是注入式抽象，没有真实远端 transport、鉴权与幂等保护实现。
- 当前没有 UI 层冲突提示或失败恢复入口，因此用户层恢复闭环仍未形成。
- 当前没有离在线切换 smoke 与冲突演练记录，无法把 `CP08-4` 整体提升到 `L3/L4`。

## 下一轮建议

1. 在不扩大 UI 面的前提下，把 executor 接到真实运行时调度边界。
2. 定义并验证远端回执应用语义，明确成功回执如何更新 cursor 与本地状态。
3. 补最小冲突恢复入口和离在线切换 smoke，推动 `CP08-4` 从 `L2` 进入 `L3`。
