# Step 06 启动恢复 Smoke 审计 - 2026-04-13

## 结论

- `CP06-4 / 启动恢复 smoke test = L4`
- `Step 06 = L4`

## 审计范围

本轮只审计 `Step 06` 剩余缺口 `CP06-4`，目标不是继续扩展离线产品能力，而是为现有启动恢复链路补齐可重复运行的 smoke 证据：

1. `useNotesWorkspaceStore.initialize()`
2. `workspaceOrchestrator.initializeWorkspace(...)`
3. `localStore.loadWorkspace()`
4. `resolveNotesWorkspaceRecoveredDrafts(...)`
5. `activeRecoveredDraft` 选中结果

## 本次交付

### 新增 contract

- `sdkwork-notes-pc-react/scripts/workspace-startup-recovery-smoke.contract.test.mjs`

该 contract 通过真实的 store 初始化链路，验证本地草稿恢复在启动阶段的最小可用性。测试使用真实的：

- `createNotesWorkspaceStore(...)`
- `createNoteWorkspaceOrchestrator(...)`
- `createBrowserNotesLocalStore(...)`
- `resolveNotesLocalWorkspaceSnapshot(...)`

同时对远端 workspace service 使用最小 stub，确保 contract 聚焦于“启动恢复是否可靠”，而不是 UI 或网络细节。

### 根脚本门禁

- `sdkwork-notes-pc-react/package.json`
- `sdkwork-notes-pc-react/scripts/package-scripts-contract.test.mjs`

`workspace-startup-recovery-smoke.contract.test.mjs` 已加入 `test:workspace:contracts`，避免后续回归时被遗漏。

## 覆盖矩阵

本轮 smoke contract 覆盖以下启动场景：

1. 当前版本 envelope 且活动 live note 存在本地草稿
2. legacy raw snapshot 直接落在 storage 中
3. unknown version payload
4. corrupted payload
5. `localStore.loadWorkspace()` 直接抛错
6. 已进 trash 或远端已不存在的草稿在启动时被过滤

## 审计判断

### 通过项

1. 启动链路已经具备恢复能力，本轮不需要再改生产实现。
2. 现有 `notes-local` schema 兼容能力能够被启动恢复直接消费。
3. 启动阶段对异常 payload 和读取失败的降级行为稳定，能够回退为空恢复快照。
4. `activeRecoveredDraft` 会跟随启动时解析出的活动 note 正确落位。

### 剩余风险

1. 本轮 smoke 证明的是“启动恢复链路存在且稳定”，不是新的 UI 交互验收。
2. 页面展示、恢复横幅交互和恢复后的编辑器行为，继续由既有 `workspace-local-recovery.contract.test.mjs` 和页面边界 contract 兜底。

## 验证记录

```powershell
node --test --experimental-test-isolation=none scripts/workspace-startup-recovery-smoke.contract.test.mjs
node --test --experimental-test-isolation=none scripts/workspace-local-schema.contract.test.mjs
node --test --experimental-test-isolation=none scripts/workspace-local-recovery.contract.test.mjs
node --test --experimental-test-isolation=none scripts/package-scripts-contract.test.mjs
pnpm.cmd typecheck
```
