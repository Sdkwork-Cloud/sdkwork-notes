# Step 05 串行保存编排审计

- 日期：`2026-04-13`
- 阶段：`Step 05 / L3`
- 波次：`Wave-B / 第二十九轮推进`
- 本轮主题：`save queue 串行编排与高风险刷盘等待`

## 1. 本轮目标

本轮承接上一轮已经完成的“失败反馈与重试状态机”能力，继续解决保存链中的下一个主阻塞：虽然 `flushDraft` 已成为唯一保存入口，但连续保存请求仍可能并发交叠，高风险动作在 `saving / retrying` 期间也缺少明确的等待语义，导致保存链还不具备真正稳定的串行编排能力。

本轮聚焦的真实缺口如下：

1. `persistActiveNote()` 在已有 in-flight save 时，无法显式区分“等待当前请求完成”和“要求当前请求完成后立即回放一次新保存”。
2. 切换笔记、创建笔记、删除文件夹、移入回收站等高风险动作，在 `saveState === 'saving' / 'retrying'` 时可能直接越过保存链，而不是等待当前请求落稳。
3. 旧请求返回时会把较早的请求快照重新写回 `activeNote`，存在覆盖更新本地草稿的风险。

## 2. 实际完成

1. `sdkwork-notes-pc-react/packages/sdkwork-notes-notes/src/services/noteWorkspaceSaveQueue.ts`
   - 新增 `createNotesWorkspaceSaveQueue()`，冻结 `hasActiveRequest / waitForActiveRequest / requestReplay / run` 四类串行编排能力。
   - 新增 `resolveNotesWorkspaceSaveCompletion()`，把“旧保存响应返回时如何保护更新草稿”的收口逻辑从 store 中抽离为独立 helper。
   - 当前队列语义为：同一活跃笔记仅允许一个远端保存请求在飞行；若飞行期间再次触发保存，只合并为一次 replay；若当前请求失败，不自动继续 replay。
2. `sdkwork-notes-pc-react/packages/sdkwork-notes-notes/src/store/useNotesWorkspaceStore.ts`
   - 新增 `activeNoteSaveQueue = createNotesWorkspaceSaveQueue()`，把活跃笔记保存链收敛到单一串行队列。
   - `persistUnsavedActiveNoteIfNeeded()` 改为：
     - `saveState === 'dirty' / 'error'` 时立即 flush
     - `saveState === 'saving' / 'retrying'` 时等待当前请求完成
   - `persistActiveNote()` 改为：
     - 若已有 active request 且当前仍是 `dirty / error`，则调用 `requestReplay()`
     - 若已有 active request 且当前不是 `dirty / error`，则调用 `waitForActiveRequest()`
   - 保存成功后改为统一消费 `resolveNotesWorkspaceSaveCompletion()`：
     - 仅在当前草稿仍与请求快照一致时，把 `activeNote` 更新为保存后的版本
     - 若本地已有更新编辑，则只更新 `persistedActiveNote`，并保持 `activeNote` 为更新草稿且 `saveState = dirty`
3. `sdkwork-notes-pc-react/scripts/workspace-save-queue.contract.test.mjs`
   - 新增 save queue contract。
   - 冻结“串行保存 + replay 合并 + 旧响应不覆盖新草稿 + store 必须消费 save queue”四项架构事实。
4. `sdkwork-notes-pc-react/scripts/workspace-high-risk-flush-boundary.contract.test.mjs`
   - 升级高风险刷盘 contract。
   - 冻结“`dirty / error` 必须 flush、`saving / retrying` 必须 wait”这一新边界。
5. `sdkwork-notes-pc-react/package.json`
   - 将 `workspace-save-queue.contract.test.mjs` 与 `workspace-high-risk-flush-boundary.contract.test.mjs` 纳入 `test:workspace:contracts` 聚合链。
6. `sdkwork-notes-pc-react/scripts/package-scripts-contract.test.mjs`
   - 同步冻结新的 contract 聚合链，避免脚本门禁回退。

## 3. Contract 驱动与验证结果

本轮继续使用 contract 驱动收口：

1. `workspace-save-queue.contract.test.mjs` 首次暴露的缺口是：
   - 仓库中不存在独立的 save queue 服务
   - store 在已有 in-flight 请求时没有 replay/wait 分流
   - 旧保存响应会重新覆盖更新的本地草稿
2. `workspace-high-risk-flush-boundary.contract.test.mjs` 升级后暴露的缺口是：
   - 旧逻辑只覆盖 `dirty / error`，没有显式要求 `saving / retrying` 等待当前请求完成
3. 完成最小实现后，重新执行以下验证并全部通过：

```powershell
node --test --experimental-test-isolation=none scripts/workspace-save-queue.contract.test.mjs
node --test --experimental-test-isolation=none scripts/workspace-save-feedback.contract.test.mjs
node --test --experimental-test-isolation=none scripts/workspace-save-flush-boundary.contract.test.mjs
node --test --experimental-test-isolation=none scripts/workspace-high-risk-flush-boundary.contract.test.mjs
node --test --experimental-test-isolation=none scripts/package-scripts-contract.test.mjs
pnpm.cmd --filter @sdkwork/notes-notes typecheck
pnpm.cmd typecheck
```

补充说明：

1. 当前环境下包内 Vitest 仍受 `spawn EPERM` 限制，因此本轮结论仍以 Node contract + TypeScript 编译门禁为准。
2. 本轮通过的不是“某几个局部用例”，而是串行保存编排、高风险刷盘边界、保存反馈模型与脚本聚合链的组合验证。

## 4. 架构判断

本轮完成后，Step 05 的保存链职责边界进一步稳定为：

1. `noteWorkspaceAutosave.ts` 与 `noteWorkspaceAutosaveRuntime.ts`
   - 负责“何时可以 schedule / flush”以及 `visibilitychange(hidden) / pagehide` 等触发器装配。
2. `noteWorkspaceSaveFeedback.ts`
   - 负责“用户当前看到什么保存状态”的可见反馈模型。
3. `noteWorkspaceSaveQueue.ts`
   - 负责“真实保存请求如何串行执行、如何合并 replay、如何等待 in-flight request、如何保护更新草稿”。
4. `useNotesWorkspaceStore.ts`
   - 负责真实持久化调用、状态落地与高风险动作前的一致性控制。

这意味着 Step 05 现在已经同时具备：

1. 单一 `flushDraft` 入口
2. 显式 `error -> retrying -> recovered` 反馈状态机
3. `save queue` 串行编排与 replay 合并
4. 高风险动作前的 `dirty/error -> flush`、`saving/retrying -> wait` 规则
5. 旧响应不覆盖更新草稿的保护逻辑

## 5. `91` 审计影响预估

基于 `docs/step/91-Step质量审计清单与复盘模板.md` 的当前证据，本轮对 Step 05 的预估影响如下：

- 预估总分从 `84-85` 区间提升到 `88-89`
- 主要拉升来源：
  - 保存编排层边界从“只有 flush 入口”升级为“flush 入口 + 串行队列 + replay”
  - 高风险动作的等待语义正式进入 contract 门禁
  - 旧响应覆盖新草稿这一隐性丢稿风险被显式消除
  - review / 架构 / release 文档在本轮同步回写
- 仍未跨过 `90` 放行线的原因：
  - 自动退避、重试上限与保存观测尚未进入稳定设计
  - 页面关闭/异常退出的正式证据矩阵还不完整
  - 真正的本地草稿恢复仍待 `Step 06` 提供落点

## 6. 风险与未完成项

1. 当前仍只有“用户触发的立即重试”，没有自动退避、最大重试次数与观测事件。
2. `visibilitychange(hidden)`、`pagehide` 与 store 内高风险动作已经被覆盖，但页面关闭确认、异常退出和崩溃恢复仍缺少完整证据矩阵。
3. 当前可靠性证明仍以 contract + typecheck 为主，受执行环境限制，尚未补出同等质量的 Vitest/E2E 证据。

## 7. 能力闭环判定

- Step 总体等级：`Step 05 / L3`
- 增量能力：`串行保存编排 / save queue`
- 增量能力等级：`L4`
- 设计闭环：是
- 实现闭环：是
- 测试闭环：是
- 验证闭环：是
- 文档闭环：是
- 集成闭环：否
- 结论：
  - Step 05 的保存主链已经从 `L2` 提升到 `L3`。
  - 当前不能宣告 `L4`，因为自动退避、重试上限、观测事件以及异常退出到本地恢复的跨 Step 集成约束仍未冻结。

## 8. 下一轮输入

下一轮建议继续沿 Step 05 推进，优先顺序如下：

1. 在 `save queue` 基础上补齐自动退避、重试上限与保存观测。
2. 把页面关闭、异常退出与崩溃恢复证据矩阵正式沉淀为 contract / review / 架构文档。
3. 冻结 Step 06 的接入约束：本地草稿恢复必须建立在当前 `flushDraft + save feedback + save queue` 主链之上，不得绕过现有保存编排层。
