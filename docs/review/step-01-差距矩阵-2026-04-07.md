# Step 01 差距矩阵

## 1. 说明

本矩阵用于把“当前现实”映射到“目标架构”，并明确后续 Step 归属、风险等级和优先级。

差距等级定义：

- `A 类`：阻塞后续所有重构的结构性问题
- `B 类`：影响主链路正确性和可扩展性的核心问题
- `C 类`：影响交付质量、发布治理和长期维护的问题
- `D 类`：优化类问题，可进入 backlog

## 2. 差距矩阵

| 领域 | 当前现状 | 目标态 | 差距等级 | 后续主 Step | 证据 |
| --- | --- | --- | --- | --- | --- |
| 执行证据层 | 原先缺少 `docs/review/` | 每个 Step 都有审计、能力兑现和复盘 | `A` | `00` `13` | `docs/review/README.md` 与 Step 00/01 产物 |
| 共享壳与能力包落点 | 现有包边界较清晰，但目标能力包尚未引入 | 本地层、搜索、同步、历史等具备独立落点 | `A` | `02` | 当前 packages 仅有 9 个包，缺失目标能力包 |
| 会话安全与配置治理 | 认证和 core 正在变化，`.env.*` 新增但未冻结治理边界 | 敏感会话、安全存储、环境分层和配置优先级明确 | `A` | `03` | `packages/sdkwork-notes-auth`、`packages/sdkwork-notes-core`、`.env.*` |
| 初始化链与数据访问边界 | 工作区主逻辑仍集中在 `notes-notes` 页面、store、repository | 初始化、repository、selector、store 边界清晰可扩展 | `B` | `04` | `packages/sdkwork-notes-notes/src/pages`、`repository`、`store` |
| 编辑可靠性 | 已有自动保存和编辑工作区，但未看到完整失败恢复闭环 | 保存状态机、失败恢复、切换一致性稳定 | `B` | `05` | `NoteEditorPane.tsx`、`useNotesWorkspaceStore.ts` |
| 本地优先与离线草稿 | 无 `notes-local` 包，无本地权威副本 | 本地 schema、权威源、离线草稿和恢复闭环 | `A` | `06` | packages 盘点结果 |
| 全文搜索与统一检索 | 当前有命令面板能力，但无独立搜索索引包 | 统一索引 schema 和统一查询语义 | `B` | `07` | `NoteCommandPalette.tsx`、无 `notes-search` 包 |
| 同步队列与冲突恢复 | 当前仍以远端主数据和直接保存为主 | 同步状态机、队列、重试、冲突恢复 | `B` | `08` | 无 `notes-sync` 包 |
| 桌面可信发布链 | 已有 matrix workflow 与 dry-run 方向，但无签名/SBOM/来源证明闭环 | 可信发布、可追溯交付、回滚闭环 | `C` | `09` | `.github/workflows/sdkwork-notes-desktop-release.yml` |
| 质量门禁 | 已有大量 contract tests 和 unit tests，但本地验证环境不稳定，缺少高层自动化体系 | E2E、视觉、性能、安全门禁可接管主验证责任 | `A` | `10` | `scripts/*.test.mjs`、`spawn EPERM` 验证结果 |
| 大规模性能治理 | 当前尚无 10k+ 量化基线 | 启动、搜索、滚动、编辑有量化回归 | `C` | `11` | `docs/架构/10` 现状结论 |
| 版本历史与恢复 | 无 `notes-history` 包与恢复语义 | revision、diff、恢复闭环 | `B` | `12` | packages 盘点结果 |
| 发布候选与下一轮输入 | release 规范已建立，但当前还没有 Step 级版本闭环结论 | 具备 go/no-go、风险清单和 backlog | `B` | `13` | `docs/release/*` 当前仍为治理基线 |

## 3. 当前优先级排序

1. `Step 02`：共享壳层与工程骨架收敛
2. `Step 03`：会话安全与配置治理升级
3. `Step 10`：质量门禁与验证环境治理并行预埋
4. `Step 04-06`：初始化链、保存链和本地能力层
5. `Step 07-09`：搜索、同步、桌面可信发布链

## 4. 结论

- 当前最应避免的错误，是在包落点和安全治理还未冻结前，直接推进本地层、同步层和版本历史层
- 当前最优路径仍然是：`02 -> 03 -> 10(预埋) -> 04 -> 05 -> 06`
