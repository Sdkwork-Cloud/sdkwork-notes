# Step 05 失败反馈与重试状态机审计

- 日期：`2026-04-07`
- 阶段：`Step 05 / L2`
- 波次：`Wave-B / 第二十七轮推进`
- 本轮主题：`保存失败反馈与重试状态机`

## 1. 本轮目标

本轮承接上一轮已经完成的“统一刷盘入口”能力，继续解决保存链中的第三个关键结构问题：保存失败后虽然可以再次触发 `persistActiveNote()`，但 store、编辑器、错误横幅与国际化文案之间并没有形成同一份显式状态机与用户反馈模型。

本轮聚焦的真实缺口如下：

1. `saveState` 只区分 `saving / saved / error`，无法准确表达“失败后的重试中”和“重试成功后已恢复”。
2. `NoteEditorPane.tsx` 与 `NotesWorkspacePage.tsx` 分别依赖零散的 `saveState` 判断，保存反馈逻辑没有统一模型。
3. 错误横幅虽然能显示失败信息，但没有与 store 状态机对齐的重试动作入口。
4. `zh-CN` 未补齐新的保存反馈文案，中文环境会退回英文键值或缺失翻译。

## 2. 实际完成

1. `sdkwork-notes-pc-react/packages/sdkwork-notes-notes/src/services/noteWorkspaceSaveFeedback.ts`
   - 新增统一保存反馈服务。
   - 冻结 `statusKey / canManualSave / isBusy / bannerMessage / retryAvailable` 五类输出。
   - 新增 `resolveNotesWorkspaceSaveRequestState()` 与 `resolveNotesWorkspaceSaveSuccessState()`，把请求态和成功态迁移规则从 store 中抽离。
2. `sdkwork-notes-pc-react/packages/sdkwork-notes-notes/src/types/notesWorkspace.ts`
   - 将 `NoteSaveState` 扩展为：`idle / dirty / saving / saved / error / retrying / recovered`。
3. `sdkwork-notes-pc-react/packages/sdkwork-notes-notes/src/store/useNotesWorkspaceStore.ts`
   - 保存请求时改为：
     - `dirty -> saving`
     - `error -> retrying`
   - 保存成功时改为：
     - `saving -> saved`
     - `retrying / error -> recovered`
   - 保持 delayed autosave 仍只由 `dirty` 进入计划，`retrying / recovered` 不自动重启新的延迟调度。
4. `sdkwork-notes-pc-react/packages/sdkwork-notes-notes/src/components/NoteEditorPane.tsx`
   - 改为消费统一 `saveFeedback` 模型。
   - 状态徽标、保存按钮禁用条件与 loading 表达不再依赖页面内的零散条件判断。
5. `sdkwork-notes-pc-react/packages/sdkwork-notes-notes/src/pages/NotesWorkspacePage.tsx`
   - 顶部错误横幅改为消费 `saveFeedback.bannerMessage`。
   - 新增 `retryLabel={t('notes.actions.retrySave')}`。
   - 仅在 `saveFeedback.retryAvailable` 为真时挂接 `onRetry={flushDraft}`。
6. `sdkwork-notes-pc-react/packages/sdkwork-notes-notes/src/components/NotesWorkspaceErrorBanner.tsx`
   - 扩展为支持可选重试按钮，形成错误反馈与重试命令的统一边界。
7. `sdkwork-notes-pc-react/packages/sdkwork-notes-i18n/src/resources/en-US.ts`
   - 新增 `notes.actions.retrySave`
   - 新增 `notes.editor.status.retrying`
   - 新增 `notes.editor.status.recovered`
8. `sdkwork-notes-pc-react/packages/sdkwork-notes-i18n/src/resources/zh-CN.ts`
   - 同步补齐 `重试保存 / 正在重试保存 / 保存已恢复`，保证中文环境文案闭环。
9. `sdkwork-notes-pc-react/scripts/workspace-save-feedback.contract.test.mjs`
   - 新增保存反馈 contract。
   - 冻结“显式重试状态机 + 统一 UI 反馈模型”这一架构事实。
10. `sdkwork-notes-pc-react/scripts/workspace-page-error-banner-boundary.contract.test.mjs`
    - 同步升级断言，要求错误横幅支持 retry CTA。
11. `sdkwork-notes-pc-react/package.json`
    - 将 `workspace-save-feedback.contract.test.mjs` 纳入 `test:workspace:contracts`。
12. `sdkwork-notes-pc-react/scripts/package-scripts-contract.test.mjs`
    - 同步冻结新的 contract 聚合链。

## 3. 先失败后通过的证据

本轮继续严格遵循 contract 驱动闭环：

1. 先新增 `workspace-save-feedback.contract.test.mjs`，要求：
   - `NoteSaveState` 必须包含 `retrying / recovered`
   - 保存请求态与成功态必须通过显式 helper 解析
   - UI 反馈必须由统一 `saveFeedback` 模型提供
2. 同步升级 `workspace-page-error-banner-boundary.contract.test.mjs`，要求错误横幅支持 retry CTA。
3. 首次执行时，失败原因准确指向以下事实：
   - `saveState` 尚未具备显式 retry lifecycle
   - 页面与编辑器仍各自做零散保存状态判断
   - 错误横幅尚未暴露与 store 对齐的重试入口
4. 完成最小实现后，重新执行以下验证并全部通过：

```powershell
node --test --experimental-test-isolation=none scripts/workspace-save-feedback.contract.test.mjs
node --test --experimental-test-isolation=none scripts/workspace-page-error-banner-boundary.contract.test.mjs
node --test --experimental-test-isolation=none scripts/package-scripts-contract.test.mjs
pnpm.cmd --filter @sdkwork/notes-notes typecheck
pnpm.cmd typecheck
```

## 4. 架构判断

本轮完成后，保存失败反馈链的职责边界稳定为：

1. `noteWorkspaceSaveFeedback.ts`
   - 负责保存反馈模型与状态机迁移规则。
   - 不关心 DOM 绑定，也不直接发起持久化。
2. `useNotesWorkspaceStore.ts`
   - 负责保存链真实执行与状态落地。
   - 只消费状态迁移 helper，不再内嵌重试状态语义。
3. `NoteEditorPane.tsx`
   - 只消费统一反馈模型。
   - 不再自行拼装“按钮是否可点、是否 loading、状态文案 key 是什么”。
4. `NotesWorkspacePage.tsx` 与 `NotesWorkspaceErrorBanner.tsx`
   - 共同构成“错误可见 + 重试命令暴露”的 UI 边界。
   - 页面只在 `retryAvailable` 为真时暴露 `flushDraft`。

这意味着 Step 05 已完成第三条主链事实：保存失败不再只是一个孤立的 `error` 标记，而是具备“错误 -> 重试中 -> 恢复”的显式可见状态机。

## 5. 已控制风险

1. `error` 状态不再是“只显示失败但语义不完整”的半闭环状态。
2. 编辑器保存按钮、状态徽标与错误横幅不再各自维护不同的保存语义。
3. 中文环境的新增保存反馈文案已补齐，不会在商业化交付场景中出现英文回退。
4. 新 contract 已进入 `test:workspace:contracts` 主链，后续若有人移除 `retrying / recovered` 或把重试按钮改回页面本地散落逻辑，会在总门禁中直接暴露。

## 6. 仍未完成项

1. 当前仍没有真正的 save queue，连续失败或并发保存尚未进入串行排队模型。
2. 重试策略仍然是“用户触发立即重试”，还没有自动退避、最大重试次数与观测事件。
3. 切换笔记、危险操作、页面关闭前三类高风险场景的 flush 证据矩阵仍未闭环。

## 7. 能力闭环判定

- Step 总体等级：`Step 05 / L2`
- 子能力编号：`CP05-3 / 失败反馈与重试策略`
- 子能力等级：`L4`
- 设计闭环：是
- 实现闭环：是
- 测试闭环：是
- 验证闭环：是
- 文档闭环：是
- 集成闭环：是
- 结论：
  - “失败反馈与重试状态机”这一子能力已经具备 `L4` 级闭环证据。
  - 但 Step 05 的整体目标仍未完成，当前不能把 Step 05 整体从 `L2` 提前提升到完成态。

## 8. 下一轮输入

下一轮建议继续沿 Step 05 推进，优先顺序如下：

1. 为“切换笔记 / 危险操作 / 页面关闭前”补齐统一的高风险场景 flush 证据矩阵。
2. 在共享 `flushDraft` 入口之上引入真正的 save queue，避免连续保存互相覆盖。
3. 在当前 `retrying / recovered` 语义上继续扩展自动退避、重试上限与观测事件。
