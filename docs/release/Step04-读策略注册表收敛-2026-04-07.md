# Step 04 发布说明：读策略注册表收敛

- 日期：2026-04-07
- Step：04
- 发布主题：repository 未来读策略注册表与按 key 选策略能力落地
- 当前结论：`Step 04 继续进行中 / L3`

## 1. 本轮交付

1. 新增 `noteWorkspaceReadStrategyRegistry.ts`
2. `noteRepository.ts` 支持 `workspaceReadStrategies` 与 `workspaceReadStrategyKey`
3. `notesWorkspace.ts` 显式定义 `NoteWorkspaceReadStrategyKey`
4. 新增 `workspace-read-strategy-registry.contract.test.mjs`
5. `test:workspace:contracts` 接入新 contract

## 2. 用户侧价值

- 当前版本虽然仍走远端快照读取，但已经具备未来切换到本地副本、读穿缓存或排队同步快照的稳定接入位
- repository 不再需要在未来每次引入新读取模式时重复改骨架
- 架构扩展风险从“口头约定”变成“可验证能力”

## 3. 验证结果

```powershell
node --test --experimental-test-isolation=none scripts/workspace-read-strategy-registry.contract.test.mjs
node --test --experimental-test-isolation=none scripts/workspace-read-strategy.contract.test.mjs
node --test --experimental-test-isolation=none scripts/workspace-data-source.contract.test.mjs
node --test --experimental-test-isolation=none scripts/package-scripts-contract.test.mjs
pnpm.cmd --filter @sdkwork/notes-notes typecheck
pnpm.cmd typecheck
```

## 4. 风险与说明

- 当前本机 `vitest` 仍受既有 `spawn EPERM` 环境限制影响，未作为本轮验收门禁
- 本轮不宣称 Step 04 完成，原因是页面容器仍保留少量 icon/action/layout 胶水

## 5. 下一轮建议

继续停留在 Step 04，优先抽离页面容器剩余的 page chrome 装配逻辑，再评估是否具备 `L4` 条件。
