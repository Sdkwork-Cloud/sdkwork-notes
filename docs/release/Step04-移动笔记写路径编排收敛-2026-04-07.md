# Step04-移动笔记写路径编排收敛-2026-04-07

## 发布摘要

本轮继续执行 `Step 04-工作区边界收敛与数据访问抽象`，新增的真实发布增量为：将 `moveNote` 写路径从 store 内联编排下沉到 `noteWorkspaceWriteCoordinator.ts`。

## 变更内容

1. `packages/sdkwork-notes-notes/src/services/noteWorkspaceWriteCoordinator.ts`
   - 新增 `NoteWorkspaceMoveNoteStateResult`
   - 新增 `moveNote` 依赖
   - 新增 `moveNoteState()`
2. `packages/sdkwork-notes-notes/src/store/useNotesWorkspaceStore.ts`
   - `moveNote()` 改为消费 write coordinator
3. `sdkwork-notes-pc-react/scripts/workspace-write-path.contract.test.mjs`
   - 新增 move note 写路径合同
   - 明确验证“move plan 预判 -> 远程移动 -> store 应用”的协作链

## 验证结果

已通过：

```powershell
node --test --experimental-test-isolation=none scripts/workspace-write-path.contract.test.mjs
pnpm.cmd --filter @sdkwork/notes-notes typecheck
pnpm.cmd typecheck
```

补充说明：

- 包内 Vitest 在当前环境仍受 `spawn EPERM` 限制，属于已知环境问题。

## 当前状态

本轮发布后：

1. write coordinator 已覆盖 `createNote / createFolder / renameFolder / moveNote`
2. Step 04 仍维持 `L3`
3. 下一优先级转为 repository 策略接缝与页面容器胶水收口

## 商业化交付影响

该增量进一步提升了以下工程能力：

1. 使主写路径的编排模式保持一致，降低后续复杂功能扩展的认知成本。
2. 强化笔记移动后 parentId、activeNote 和 expandedFolderIds 的一致性保证。
3. 为未来离线副本、同步重放和冲突处理留出更清晰的 mutation 协调边界。
