# Step04-重命名文件夹写路径编排收敛-2026-04-07

## 发布摘要

本轮继续执行 `Step 04-工作区边界收敛与数据访问抽象`，新增的真实发布增量为：将 `renameFolder` 写路径从 store 内联编排下沉到 `noteWorkspaceWriteCoordinator.ts`。

## 变更内容

1. `packages/sdkwork-notes-notes/src/services/noteWorkspaceWriteCoordinator.ts`
   - 新增 `NoteWorkspaceRenameFolderStateResult`
   - 新增 `renameFolder` 依赖
   - 新增 `renameFolderState()`
2. `packages/sdkwork-notes-notes/src/store/useNotesWorkspaceStore.ts`
   - `renameFolder()` 改为消费 write coordinator
3. `sdkwork-notes-pc-react/scripts/workspace-write-path.contract.test.mjs`
   - 新增 rename folder 写路径合同
   - 明确验证“远程重命名 -> 本地状态计划 -> store 应用”的协作链

## 验证结果

已通过：

```powershell
node --test --experimental-test-isolation=none scripts/workspace-write-path.contract.test.mjs
pnpm.cmd --filter @sdkwork/notes-notes typecheck
pnpm.cmd typecheck
```

补充说明：

- 包内 Vitest 在当前环境仍受 `spawn EPERM` 限制，属于已知环境问题。

## 当前状态

本轮发布后：

1. write coordinator 已覆盖 `createNote / createFolder / renameFolder`
2. Step 04 仍维持 `L3`
3. 下一优先级明确为 `moveNote` 写路径下沉

## 商业化交付影响

该增量提升了以下工程能力：

1. 降低 store 与远程写路径编排的耦合度。
2. 强化重命名文件夹后 descendant / note parent / selection / expansion 的一致性保证。
3. 为后续商业化版本中的更复杂 mutation 协调、离线同步与冲突处理预留更清晰的服务边界。
