# 06. 业务流程 / 应用接口与集成设计补充
## 顶部动作适配边界补充 - 2026-04-07

## 1. 本次补充目的

补充工作区页面在顶部动作区链路上的最终适配边界设计，明确以下职责不再停留在页面容器中：

1. `NotesWorkspacePageHeaderAction` descriptor 到最终 `Link/Button` 节点的 UI 绑定。
2. `iconKey -> LucideIcon` 的顶部动作视图绑定。
3. command descriptor 到点击 `onCommandAction(command)` 闭包的最终适配。

## 2. 收敛后的职责链路

### 2.1 模型层

- `noteWorkspacePageChrome.ts`
  - 负责输出 `NotesWorkspacePageHeaderAction[]`。
  - 负责提供 `resolveNotesWorkspaceChromeIcon()`。
  - 只描述动作语义、状态化标签与 iconKey，不承担 React 视图渲染职责。

### 2.2 组件适配边界

- `NotesWorkspaceHeaderActions.tsx`
  - 负责消费 `NotesWorkspacePageHeaderAction[]`。
  - 负责把 `iconKey` 解析为真实图标组件。
  - 负责把 `kind === 'link'` 与 `kind === 'command'` 分流为最终 `Link` / `Button` 节点。
  - 负责把 `onCommandAction(action.command)` 绑定为最终点击闭包。

### 2.3 页面容器

- `NotesWorkspacePage.tsx`
  - 只负责生成 `headerActions` descriptor。
  - 只负责提供命令执行入口 `handleWorkspacePageCommand`。
  - 不再在页面层执行 `headerActions.map(...)`、`action.kind === 'link'` 分支或 `resolveNotesWorkspaceChromeIcon()` 调用。

## 3. 评估标准

满足以下条件，才视为顶部动作适配边界达标：

1. 页面容器中不再出现 `headerActions.map(...)`。
2. 页面容器中不再出现 `action.kind === 'link'` 的最终视图分支。
3. 页面容器中不再出现 `resolveNotesWorkspaceChromeIcon()` 的顶部动作视图解析。
4. `NotesWorkspaceHeaderActions.tsx` 必须显式完成 `resolveNotesWorkspaceChromeIcon()`、`Link/Button` 分流与 `onCommandAction(action.command)` 绑定。
5. 边界 contract 必须纳入 `test:workspace:contracts` 总门禁。

## 4. 当前结论

本次补充完成后：

- 顶部动作区已从页面容器残留阻塞列表中移除。
- 工作区页面进一步接近“模型 / runtime / 组件适配 / 页面装配”四层分离目标。
- Step 04 仍保持 `L3`，因为页面层剩余本地渲染胶水是否还能继续下沉，仍需下一轮复核。
