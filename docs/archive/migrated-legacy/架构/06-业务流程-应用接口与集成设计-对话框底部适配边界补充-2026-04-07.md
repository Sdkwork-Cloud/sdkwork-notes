# 06. 业务流程 / 应用接口与集成设计补充
## 对话框底部适配边界补充 - 2026-04-07

## 1. 本次补充目的

补充工作区页面在 `Dialog footer` 链路上的最终适配边界设计，明确以下职责不再停留在页面容器中：

1. `cancelLabel / confirmLabel` 到底部按钮文案的最终 JSX 绑定。
2. `closeDialog()` 与 `confirmDialog()` 到 cancel/confirm 按钮点击闭包的最终绑定。
3. `ghost / danger` 按钮样式在对话框底部区域的最终适配。

## 2. 收敛后的职责链路

### 2.1 状态与 runtime 层

- `buildNotesWorkspaceDialogState()`
  - 负责输出 `title / description / confirmLabel`。
- `createNotesWorkspaceDialogRuntime()`
  - 负责输出 `closeDialog()` 与 `confirmDialog()`。

### 2.2 组件适配边界

- `NotesWorkspaceDialogFooter.tsx`
  - 负责消费 `cancelLabel / confirmLabel / onCancel / onConfirm`。
  - 负责把 cancel 按钮适配为 `ghost`。
  - 负责把 confirm 按钮适配为 `danger`。

### 2.3 页面容器

- `NotesWorkspacePage.tsx`
  - 只负责向 `Dialog` 注入 `footer={<NotesWorkspaceDialogFooter ... />}`。
  - 不再在页面层直接装配 `Button appearance="ghost"`、`Button appearance="danger"` 与 fragment footer JSX。

## 3. 评估标准

满足以下条件，才视为对话框底部适配边界达标：

1. 页面容器中不再出现 `footer={(<>...</>)}` 形式的本地 JSX 装配。
2. 页面容器中不再出现 `Button appearance="ghost"` 与 `Button appearance="danger"` 的对话框底部视图绑定。
3. `NotesWorkspaceDialogFooter.tsx` 必须显式完成 cancel/confirm 文案与按钮点击行为绑定。
4. `components/index.ts` 必须导出 `NotesWorkspaceDialogFooter`，确保边界入口稳定。
5. `workspace-page-dialog-footer-boundary.contract.test.mjs` 必须纳入 `test:workspace:contracts` 总门禁。
6. 包级与根级 TypeScript 校验必须通过，避免边界抽离后出现类型回退。

## 4. 当前结论

本次补充完成后：

1. 工作区页面中上一轮审计定义的高优先级本地视图胶水已全部移出页面容器。
2. `NotesWorkspacePage.tsx` 已回到“状态输入 + runtime 注入 + 布局装配”的薄容器职责。
3. Step 04 已达到 `L4`，下一执行入口应切换到 `Step 05-编辑器与自动保存可靠性升级`。
