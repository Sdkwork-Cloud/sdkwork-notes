# Step 08 - 主写入路径接入收口

- 日期：`2026-04-13`
- 波次：`Wave-C / 第四十六轮推进`
- 检查点：`CP08-3 / 主写入路径接入`
- 本轮收口：`permanent-delete + toggleFavorite + clearTrash`

## 交付范围

- 扩展 `@sdkwork/notes-sync` 的 note operation 集，引入独立 `permanent-delete` 语义。
- 在 `deleteNotePermanently()` 成功路径追加 `note / permanent-delete / queued` 同步任务。
- 在 `toggleFavorite()` 的 clean-save 成功路径追加 `note / upsert / queued` 同步任务，补齐 note 级关键元数据变更。
- 在 `clearTrash()` 成功路径把批量清空废纸篓拆解为多条 `note / permanent-delete / queued` 任务，并要求一次 queue snapshot 持久化完成。
- 不在本轮提前实现 worker、远端回执应用、冲突 UI 或离在线切换 smoke。

## 变更摘要

### 新增

- `sdkwork-notes-pc-react/scripts/workspace-sync-state-machine.contract.test.mjs`
  - 显式冻结 `permanent-delete` 为 Step 08 一期的独立 operation。
- `sdkwork-notes-pc-react/scripts/workspace-sync-queue.contract.test.mjs`
  - 新增 `permanent-delete` queue snapshot 合同。
- `sdkwork-notes-pc-react/scripts/workspace-sync-write-path.contract.test.mjs`
  - 新增 `deleteNotePermanently -> queued permanent-delete sync task`
  - 新增 `toggleFavorite -> queued upsert sync task`
  - 新增 `clearTrash -> queued permanent-delete sync tasks`

### 修改

- `sdkwork-notes-pc-react/packages/sdkwork-notes-sync/src/index.ts`
  - operation 集扩展为 `upsert / delete / restore / move / permanent-delete`。
- `sdkwork-notes-pc-react/packages/sdkwork-notes-notes/src/store/useNotesWorkspaceStore.ts`
  - 新增 `enqueueNotesSyncTasks(...)` 批量 helper，并保留 `enqueueNoteSyncTask(...)` / `enqueueNoteUpsertSyncTask(...)` 作为单条封装。
  - `deleteNotePermanently()` 成功路径新增 `note/permanent-delete` 入队。
  - `toggleFavorite()` 成功路径新增 `note/upsert` 入队。
  - `clearTrash()` 成功路径新增批量 `note/permanent-delete` 入队。

## 兑现结果

- `delete` 不再兼任“软删除 + 永久删除”两种事实；`moveNoteToTrash` 与 `deleteNotePermanently` 的同步语义已经显式分叉。
- `toggleFavorite` 不再停留在“远端已保存但队列无事实”的漏洞状态，而是被收口为 `upsert` 类元数据更新。
- `clearTrash` 不再是 `CP08-3` 的未覆盖批量写动作，而是已被映射为一组可追踪的 per-note 永久删除任务。
- `CP08-3` 当前已经覆盖：
  - `createNote`
  - `persistActiveNote`
  - `toggleFavorite`
  - `moveNoteToTrash`
  - `restoreNoteFromTrash`
  - `moveNote`
  - `deleteNotePermanently`
  - `clearTrash`

## 当前状态

- `CP08-1 / 同步任务模型与状态机冻结 = L4`
- `CP08-2 / 待同步队列与重试机制落地 = L4`
- `CP08-3 / 主写入路径接入 = L4`
- `CP08-4 / 冲突与失败恢复验证 = 未开始`
- `Step 08` 当前整体仍为 `L2`

## 仍缺失项

- 仍未交付后台同步 worker、远端回执应用、冲突提示和失败恢复交互。
- 仍未完成离在线切换验证，因此 Step 08 不能提升到 `L3/L4`。
- 文件夹类写路径与更复杂的结构冲突仍留给后续阶段处理，不作为本轮收口阻塞。

## 验证

```powershell
node --test --experimental-test-isolation=none scripts/workspace-sync-state-machine.contract.test.mjs
node --test --experimental-test-isolation=none scripts/workspace-sync-queue.contract.test.mjs
node --test --experimental-test-isolation=none scripts/workspace-sync-write-path.contract.test.mjs
pnpm.cmd --filter @sdkwork/notes-sync typecheck
pnpm.cmd --filter @sdkwork/notes-notes typecheck
pnpm.cmd typecheck
```
