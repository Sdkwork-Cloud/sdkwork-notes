# 96. Step 并行执行周计划与排班建议

## 1. 文档定位

本文件用于把 `94` 的并行执行编排，进一步落到“周节奏、排班、检查点、收口动作”层面，便于多团队或多子 Agent 按固定节奏推进。

## 2. 适用前提

本排班建议基于以下假设：

- 主工程仍为 `sdkwork-notes-pc-react`
- 当前阶段以重构、升级、治理为主，不是横向大功能扩张
- 能够安排统一集成 Owner
- 每周至少能完成一次集成和一次审计

## 3. 角色建议

最少角色配置：

| 角色 | 主要职责 |
| --- | --- |
| 集成 Owner | 统一边界拍板、处理冲突、收口验证 |
| 骨架/平台 Owner | 壳层、配置、安全、脚本、workflow |
| 工作区 Owner | 初始化、编辑器、本地存储、版本历史 |
| 数据能力 Owner | 搜索、同步、索引、状态机 |
| 质量 Owner | 测试、性能基准、审计、回归 |

扩展角色：

- 桌面发布 Owner
- 文档与审计 Owner

## 4. 周节奏总原则

推荐周节奏：

- 周一：冻结本周目标、输入、车道分配
- 周二到周三：并行执行
- 周四：集成、补测试、修冲突
- 周五：审计、回写、形成下一周输入

禁止做法：

- 周内没有任何中期检查点
- 多车道直到周五才首次集成
- 验证和文档回写全部压到最后一天

## 5. 推荐周计划

### 5.1 第 1 周：`Wave-A-1`

目标：

- 完成 `00`
- 完成 `01`
- 启动 `02` `03` 设计冻结

排班建议：

- 车道 `W1-A1`：差距基线与审计
- 车道 `W1-A2`：共享壳层、目录骨架
- 车道 `W1-A3`：会话安全与配置治理
- 车道 `W1-A4`：contract test、冒烟与审计模板

检查点：

- `CP-W1-1`：差距矩阵冻结
- `CP-W1-2`：壳层边界与安全治理方案冻结

### 5.2 第 2 周：`Wave-A-2 / Wave-B-1`

目标：

- 收口 `02`
- 收口 `03`
- 启动 `04`

排班建议：

- 车道 `W2-B1`：AppRoot / provider / 路由边界收敛
- 车道 `W2-B2`：安全存储与配置迁移实现
- 车道 `W2-B3`：工作区初始化链梳理
- 车道 `W2-B4`：验证车道补充脚本和 contract test

检查点：

- `CP-W2-1`：`Wave-A` 总验收
- `CP-W2-2`：初始化链时序图与主写入目录冻结

### 5.3 第 3 周：`Wave-B-2`

目标：

- 推进 `04`
- 推进 `05`
- 启动 `06`

排班建议：

- 车道 `W3-C1`：repository/store/init orchestration
- 车道 `W3-C2`：编辑器保存状态机与失败恢复
- 车道 `W3-C3`：本地存储 schema 与草稿恢复
- 车道 `W3-C4`：初始化/保存/离线回归

检查点：

- `CP-W3-1`：工作区初始化主链路跑通
- `CP-W3-2`：编辑器可靠性基本可回归
- `CP-W3-3`：本地草稿恢复可演练

### 5.4 第 4 周：`Wave-C-1`

目标：

- 收口 `06`
- 推进 `07`
- 启动 `08`
- 启动 `09` 的发布链前置工作

排班建议：

- 车道 `W4-D1`：本地层收口与迁移验证
- 车道 `W4-D2`：搜索索引模型与统一检索入口
- 车道 `W4-D3`：同步状态机与冲突模型
- 车道 `W4-D4`：桌面 runtime / release dry-run 前置
- 车道 `W4-D5`：搜索/同步/桌面专项验证

检查点：

- `CP-W4-1`：本地权威源冻结
- `CP-W4-2`：搜索语义冻结
- `CP-W4-3`：同步状态机冻结

### 5.5 第 5 周：`Wave-C-2 / Wave-D-1`

目标：

- 收口 `07`
- 收口 `08`
- 收口 `09`
- 强化 `10`
- 启动 `11` `12`

排班建议：

- 车道 `W5-E1`：搜索性能与准确性
- 车道 `W5-E2`：同步冲突恢复与回放
- 车道 `W5-E3`：桌面可信发布链与打包验证
- 车道 `W5-E4`：测试门禁、视觉/E2E/安全
- 车道 `W5-E5`：性能画像与版本历史模型设计

检查点：

- `CP-W5-1`：`Wave-C` 总验收
- `CP-W5-2`：质量门禁可接管主验证责任

### 5.6 第 6 周：`Wave-D-2`

目标：

- 推进 `11`
- 推进 `12`
- 收口 `13`

排班建议：

- 车道 `W6-F1`：10k+ 工作区性能治理
- 车道 `W6-F2`：revision、diff、恢复交互
- 车道 `W6-F3`：发布候选、回滚、文档回写
- 车道 `W6-F4`：阶段审计与下一轮 backlog

检查点：

- `CP-W6-1`：性能指标达标
- `CP-W6-2`：版本恢复闭环完成
- `CP-W6-3`：发布就绪结论与下一轮输入冻结

## 6. 单周排班模板

建议在每周开始时新增：

- `docs/review/week-XX-step-排班-YYYY-MM-DD.md`

模板如下：

```md
# Week XX Step 排班

## 1. 本周目标

- 

## 2. 车道安排

| 车道 | Owner | 目标 Step | 主写入目录 | 本周检查点 |
| --- | --- | --- | --- | --- |
|  |  |  |  |  |

## 3. 统一约束

- 本周共享边界文件 Owner：
- 本周统一验证口径：
- 本周一票否决项：

## 4. 风险

- 

## 5. 周五收口输出

- 
```

## 7. 多子 Agent 排班建议

### 7.1 推荐并发数

- `Wave-A`：最多 `3-4` 个并行子 Agent
- `Wave-B`：最多 `4` 个并行子 Agent
- `Wave-C`：最多 `4-5` 个并行子 Agent
- `Wave-D`：最多 `4` 个并行子 Agent

不建议无边界地超过上述并发数，因为集成和验证成本会迅速反噬效率。

### 7.2 子 Agent 分工模式

- 一个子 Agent 对应一个车道
- 一个车道只拥有一组主写入目录
- 主 Agent 只做拍板、评审、集成与收口

### 7.3 每周中期集成

如果使用多子 Agent，建议周三必须做一次中期集成检查，至少确认：

- 写入范围是否越界
- 测试与验证是否滞后
- 是否出现共享边界冲突

### 7.4 最速执行模式推荐

为了在当前仓库中尽快完成全部 Step，建议优先使用以下三档模式：

| 模式 | 适用规模 | 推荐车道组合 | 说明 |
| --- | --- | --- | --- |
| `Fast-S` | `2` 子 Agent | `主脊柱/集成` + `功能实现` | 只做有限并行，适合高稳定性优先 |
| `Fast-M` | `4` 子 Agent | `集成/文档` + `壳层/安全` + `工作区/本地层` + `搜索/同步/测试` | 当前仓库最推荐模式 |
| `Fast-L` | `6` 子 Agent | `集成` + `壳层/安全` + `初始化/编辑/本地层` + `搜索/同步` + `桌面/发布链` + `测试/性能/历史` | 速度最快，但必须严格中期集成 |

`Fast-M` 的推荐原因：

- 共享边界冲突最少
- 能同时覆盖主业务链、平台链和质量链
- 集成成本明显低于 `Fast-L`

## 8. 周度退出标准

每周结束时，至少应形成：

- 已通过的检查点列表
- 未通过的检查点及阻塞升级记录
- 已沉淀的 `docs/review/` 审计记录
- 下一周冻结输入

## 9. 结论

好的排班不是把所有人塞进同一个大目标，而是让每周目标足够清晰、车道足够独立、检查点足够前置、周五一定能形成可审计结果。本文件用于把这个节奏固化下来。
