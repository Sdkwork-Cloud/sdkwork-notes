# Step 08 - 同步任务模型与状态机冻结
- 日期：`2026-04-13`
- 波次：`Wave-C / 第三十九轮推进`
- 检查点：`CP08-1 / 同步任务模型与状态机冻结`

## 交付范围

- 在 `@sdkwork/notes-sync` 冻结 Step 08 一期的同步任务模型、状态集合、失败分类、冲突分类与回放语义。
- 保持实现为纯模型层，不提前引入队列持久化、页面 UI 或主写入路径接线。
- 将新契约接入根级 `test:workspace:contracts`，确保后续所有 typecheck 自动守住同步状态边界。

## 变更摘要

### 新增

- `sdkwork-notes-pc-react/scripts/workspace-sync-state-machine.contract.test.mjs`
  - 冻结 `queued -> running -> retrying -> queued -> running -> completed` 生命周期
  - 冻结 `conflict` 手动恢复语义
  - 冻结失败可重试分类与非法状态迁移保护

### 修改

- `sdkwork-notes-pc-react/packages/sdkwork-notes-sync/src/index.ts`
  - 定义 `NOTES_SYNC_TASK_STATUSES / NOTES_SYNC_ENTITY_TYPES / NOTES_SYNC_OPERATION_TYPES`
  - 定义 `NOTES_SYNC_FAILURE_CODES / NOTES_SYNC_CONFLICT_CODES / NOTES_SYNC_REPLAY_MODES`
  - 新增 `createNotesSyncTask()`
  - 新增 `createNotesSyncFailure()`、`createNotesSyncConflict()`
  - 新增 `transitionNotesSyncTask()`、`resolveNotesSyncTaskReplayMode()`
  - 保留 `NotesSyncCoordinator` 作为后续 `CP08-2` 队列实现边界
- `sdkwork-notes-pc-react/package.json`
- `sdkwork-notes-pc-react/scripts/package-scripts-contract.test.mjs`

## 兑现结果

- Step 08 不再只有空占位包，`notes-sync` 已具备最小状态机语义。
- `retrying` 与 `failed / conflict` 的回放模式明确区分为 `automatic / manual`。
- 队列、重试、失败、冲突、回放的命名和状态流转已冻结，可直接作为 `CP08-2` 与 `CP08-3` 的实现前置。

## 当前状态

- `CP08-1 / 同步任务模型与状态机冻结 = L4`
- `CP08-2 / 待同步队列与重试机制 = 待开始`
- `CP08-3 / 主写入路径接入 = 待开始`
- `CP08-4 / 冲突与失败恢复验证 = 待开始`
- `Step 08` 处于 `CP08-1 已闭环、整体进行中`

## 验证

```powershell
node --test --experimental-test-isolation=none scripts/workspace-sync-state-machine.contract.test.mjs
node --test --experimental-test-isolation=none scripts/package-scripts-contract.test.mjs
pnpm.cmd --filter @sdkwork/notes-sync typecheck
pnpm.cmd typecheck
```
