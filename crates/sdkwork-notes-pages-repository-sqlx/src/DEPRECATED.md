# Deprecated

Canonical database lifecycle assets live in the application-root `database/` directory.

Use `crates/sdkwork-notes-pages-repository-sqlx/src/sqlite_core.sql` as the legacy source reference.
