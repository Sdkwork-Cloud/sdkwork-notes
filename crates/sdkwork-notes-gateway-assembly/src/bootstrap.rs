//! Generated gateway bootstrap for sdkwork-notes.
//! Regenerated by pnpm gateway:assembly:materialize when bootstrap contains todo!().

use axum::Router;

pub struct ApplicationAssembly {
    pub router: Router,
}

pub fn assemble_application_router() -> ApplicationAssembly {
    let mut router = Router::new();
    router = router.merge(sdkwork_routes_notes_app_api::gateway_mount());
    router = router.merge(sdkwork_routes_notes_backend_api::gateway_mount());
    router = router.merge(sdkwork_routes_notes_http_auth::gateway_mount());
    ApplicationAssembly { router }
}
